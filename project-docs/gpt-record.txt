# Record

## 记录格式

每条记录采用以下格式：

```markdown
## 版本号 短语说明做了什么

用 1～3 句话说明具体完成了什么、验证结果，以及必要的下一步。
```

版本号规则：

- `1.0`、`2.0`：完成一个主要阶段
- `1.1`、`1.2`：完成阶段内的一个小步骤
- 只记录已经实际完成的内容

## 1.0 建立项目规划

创建了 `Create GPT Step by Step` 项目，并制定了从字符级 tokenizer 到聊天模型扩展的学习路线。建立项目文档和本地 Git 仓库，并把首次提交推送到公开 GitHub 仓库。安装 Python 3.13.15，创建独立虚拟环境，安装 PyTorch 2.13.0 和 NumPy 2.5.2，并验证 MPS GPU 可用。

## 1.1 实现字符级 Tokenizer

读取包含 153 个字符的训练文本，建立了包含 92 个不同字符的 `stoi` 和 `itos` 双向映射。实现 `encode()` 与 `decode()`，并通过测试句和全文的无损往返测试。

## 1.2 构造训练数据

将全文编码为 `torch.long` 张量，并按 90%/10% 切分为训练集和验证集。使用长度为 8 的上下文窗口构造错开一个 token 的输入与目标，实现随机抽样及形状为 `[4, 8]` 的训练和验证 batch。

## 2.0 训练 Bigram 基线模型

建立形状为 `[92, 92]`、共 8464 个参数的 Bigram 预测表，完成 Logits、交叉熵、AdamW 参数更新和 1000 步训练，平均训练 Loss 降至约 0.71。实现从字符“小”开始的连续 100 Token 生成；平均验证 Loss 约为 4.89，观察到小数据集上的明显过拟合，也验证了 Bigram 只能参考前一个字符的能力限制。

## 3.0 实现因果 Self-Attention

把 Token Embedding 与 Position Embedding 合并为形状 `[4, 8, 32]` 的上下文输入，手动实现 Query、Key、Value、缩放点积、因果遮罩和 Value 加权汇总。将单头注意力扩展为 4 个并行 Head，验证每个 Head 输出 `[4, 8, 8]`、拼接与投影后输出 `[4, 8, 32]`，且未来 Token 的注意力概率为 0。

## 4.0 组合 Transformer Block

封装 `MultiHeadAttention`、`FeedForward` 和 Pre-Norm `TransformerBlock`，加入两次 LayerNorm、输出投影和两条残差连接。成功堆叠两个 Block，验证输入输出均为 `[4, 8, 32]`，两个 Block 的全部参数都获得非零梯度，堆栈参数总数为 25216。

## 5.0 组合完整 GPT 模型

将 Token Embedding、Position Embedding、两个 Transformer Block、最终 LayerNorm 和语言模型输出层封装为 `GPTLanguageModel`。验证输入 `[4, 8]` 能输出 `[4, 8, 92]` 的 Logits，初始交叉熵 Loss 约为 4.78，完整模型参数总数为 31516。

## 5.1 完成语料机械清洗

备份并隔离《斗破苍穹》原始语料，建立不会提交到 GitHub 的工作副本。第一阶段清洗删除了 11220 个 NUL 空字符并统一了 198068 个 CRLF 换行，输出可正常按 UTF-8 解码且没有替换字符；同时生成 JSON 报告和可独立调节的 I/O、文本变换轮换日志。

## 5.2 审计语料内容污染

新增只读内容审计脚本，在不删除正文的前提下识别来源声明、下载宣传、阅读站水印、更新提示和电子书广告。共发现 81 行候选污染，其中 30 行可整行处理、51 行广告嵌在正文中；同时记录了 1291 组重复章节标题和 20 个未出现的章节编号，供下一步保守清洗与重复内容核对。

## 5.3 审计重复章节与正文

使用章节边界指纹和连续非空段落匹配检查重复内容，在 1903 个章节区块中没有发现边界完全一致的整章副本，但发现 105 处至少连续 5 段、500 字以上的完全重复正文。105 个较晚出现的重复范围互不重叠，保守估算包含 117153 个冗余字符；抽样确认重复来自同一章节的不同抓取版本，而非正常剧情复述。

## 5.4 清除网页广告与正文水印

第二阶段清洗删除了 57 行来源声明、网页框架、导航和电子书广告，并从正文中精确移除了 51 处网站水印和更新提示，共减少 3155 个字符、保留原文的 99.9524%。独立复查对现有污染规则的残留命中为 0，四项测试确认整行广告、嵌入水印和更新横幅能够删除，同时普通正文保持不变。

## 5.5 配对近似重复章节版本

将同章节编号以及重复正文块对应到 1903 个章节区块，共比较 316 组候选并发现 135 组相似版本。采用“章节编号相同且较短版本至少 98% 被另一版覆盖”的保守标准，得到 22 组无建议冲突的高置信版本，另外 113 组保留复核；测试确认跨章节相似内容不会被标记为自动删除对象。

## 5.6 剔除高置信重复版本

在保留 stage2 清洗结果的前提下，仅根据 `chapter_version_audit.json` 的 high 置信结果，移除了 22 个重复章节版本。共清理 2563 行、76711 字符，保留率约 98.84%，并生成 `data/clean/doupo_stage3.txt` 与 `data/clean/stage3_report.json`，每段删除范围都带有章节编号与标题。

为了确保安全性，删除过程保持了章节边界完整性，并将输入/输出 SHA256 与字符数写入报告，便于下一步回审。

## 5.7 复审 Stage3 语料

对 `doupo_stage3.txt` 进行三项复审：内容污染、重复正文块、章节版本配对。复审结果为候选污染行 0、章节区块数 1881、重复正文块 101、版本配对保留 113 条但全部为 review，high 置信为 0。对应报告分别是 `content_audit_stage3.json`、`duplicate_audit_stage3.json`、`chapter_version_audit_stage3.json`。

此时语料进入“可开始重新编码训练集/验证集”的状态。

## 5.8 构建 stage3 训练数据

基于 `data/clean/doupo_stage3.txt` 生成字符级 tokenizer 张量，统计词表到 4478 个字符，产出 `data/clean/doupo_stage3_tensors.pt`。按 90/10 划分后得到训练集 5889850、验证集 654428 token（总约 6544278），并输出 `stage3_dataset_report.json` 及 `stage3_vocab.json` 供后续训练复现。

## 5.9 训练输入 Embedding 步骤

新增 `embedding_step.py`，将训练 batch 的 token ids 映射到 `token_embedding`，并添加 `position_embedding` 形成 `token + position` 的上下文向量。通过 `doupo_stage3_tensors.pt` 验证了输出形状：token 形状 `[4, 8]`、token 向量 `[4, 8, 64]`、位置向量 `[8, 64]`、融合后上下文向量 `[4, 8, 64]`，并生成 `embedding_report.json` 记录本步统计。

## 5.10 扩大训练批次与上下文并试跑 6.0 训练循环

新增 `train_gpt_stage3.py`，复用 `Transformer Block` 结构实现完整 GPT 训练前向与交叉熵损失。用 `BLOCK_SIZE=64`、`BATCH_SIZE=16` 跑了 80 步（含定期评估）后，训练损失从约 8.56 降到 6.69，验证损失同步下降到约 6.66，证明模型开始学到上下文规律。

脚本同时加入分模块日志、梯度裁剪和 checkpoint 导出（含 loss 历史），将第一批输出和参数配置记录到 `checkpoints/gpt_stage3_history.json`，下一步可继续增大 `NUM_HEADS/NUM_LAYERS` 或步数。

## 5.11 训练脚本升级为续训与生成

把 `train_gpt_stage3.py` 再升级为可控训练工具：支持 `RESUME_TRAINING=1` 从上次 checkpoint 继续训练，支持 `GEN_STEPS/GEN_START_TEXT/TOP_K/TEMPERATURE` 进行采样生成，支持自定义 checkpoint 路径。通过 10 步测试验证续训从 step 10 成功接续到 step 15，且可直接输出生成片段。

## 5.12 增加可复现训练能力与参数固定

在 `train_gpt_stage3.py` 中加入 `SEED` 与 `DETERMINISTIC` 两个开关，统一设置 torch 的随机种子（CPU 与 CUDA）并写入日志/Checkpoint 元信息，便于同一环境下复现实验轨迹。并验证了 1~5 步短跑运行可正常完成并输出 loss 曲线，当前默认已可用于后续“固定参数稳定对比”的阶段。

## 5.13 续训到 step 120 并观察 loss 降幅

基于 `checkpoints/gpt_stage3.pt` 执行续训（`RESUME_TRAINING=1`，`MAX_STEPS=120`，`BATCH_SIZE=4`，`BLOCK_SIZE=64`，`SEED=42`）并记录日志。loss 在训练集/验证集上均持续下降，约从 `step 20: 8.35/8.33` 到 `step 120: 6.35/6.35`，表明模型继续拟合语料并具备稳定学习趋势。并生成 sample 与续写文本用于检视输出质量。

## 5.14 固定 10 句 prompt 的训练后对比评估

新增 `data/prompt10_eval.txt`（10 条固定中文 prompt）并在训练中加入 `PROMPT_EVAL_*` 开关：按 `PROMPT_EVAL_EVERY=20` 在 step 140、160 采集生成结果。通过 `checkpoints/prompt_eval_stage3.json` 保存逐步输出，验证后续同一组输入在不同训练阶段的文风变化，当前样本中可见输出仍有重复噪声，训练继续改进空间。

## 5.15 续训至 step 10000 并复测固定样本

使用 `BATCH_SIZE=16`、`BLOCK_SIZE=64`、`EMBEDDING_DIM=64`、4 个注意力头和 2 个 Transformer Block，将 stage3 模型续训至 step 10000。最终平均训练 Loss 为 3.7468，平均验证 Loss 为 3.8638，两者差距约 0.117，尚未出现明显的训练集过拟合分叉。

加载 `checkpoints/gpt_stage3.pt`，在不更新参数的情况下，用固定 10 条 prompt、温度 1.0、Top-K 40 和每题 80 个新字符进行复测。输出已经明显具有《斗破苍穹》的角色、标点和叙事风格，但仍无法稳定理解问题、遵循“三句话”等指令或保持长程语义连贯；当前模型属于小说字符级续写模型，还不是问答聊天模型。

## 5.16 对比三组文本采样参数

固定 step 10000 模型、10 条 prompt、随机种子 42 和每题最多 30 个新字符，对比了保守采样（Temperature 0.5、Top-K 10）、均衡采样（0.8、20）和自由采样（1.0、40）。保守组更容易出现连续语气词和称呼重复，自由组的语义跳跃及重复同样明显；均衡组相对减少了极端重复和随机跳跃，因此暂定 `Temperature=0.8`、`Top-K=20` 为当前 stage3 模型的推荐生成参数。

本次对比也确认：采样参数只能调节随机性和重复程度，无法解决模型不理解问题的问题。下一阶段需要扩大上下文与模型容量，或在后续加入指令问答数据。

## 5.17 建立 Stage 4 扩容模型并完成 100 步试跑

保留 Stage 3 检查点不变，新建 Stage 4 模型：上下文长度由 64 扩大到 128，Embedding 维度由 64 扩大到 128，Transformer Block 由 2 层增加到 4 层，注意力头维持 4 个，Batch Size 设为 8。可训练参数从 Stage 3 的 714238 增加到 1959038；前向输出形状为 `[8, 128, 4478]`，并确认全部可训练参数均能获得梯度。

使用 CPU 从零完成 step 0~100 smoke test，每 20 步评估一次。训练 Loss 从 8.5259 降至 5.8286，验证 Loss 从 8.5384 降至 5.6711，训练过程无报错且未出现验证 Loss 分叉。模型已保存为 `checkpoints/gpt_stage4.pt`，Stage 3 的 `checkpoints/gpt_stage3.pt` 未被覆盖；100 步生成文本仍不连贯，符合新模型训练步数很少的预期。

## 5.18 实测 Mac 容量并调整为 Stage 5

在 M4、16 GB MacBook Air 上依次测试约 196 万、710 万、1773 万、2993 万、7794 万和 1.10 亿可训练参数的模型。每组均完成前向传播、Loss、反向传播、梯度裁剪和 AdamW 更新；1.10 亿参数组能够运行，但当前 PyTorch 环境无法启用 MPS，全部测试使用 CPU。完整结果记录在 `reports/mac_capacity_benchmark.md`。

结合约 654 万字符的训练语料、训练速度和过拟合风险，将推荐配置调整为 Stage 5：`BATCH_SIZE=4`、`BLOCK_SIZE=256`、`EMBEDDING_DIM=256`、`NUM_HEADS=8`、`NUM_LAYERS=6`，共 7097214 个可训练参数。Stage 5 从零完成 100 步试跑，训练 Loss 从 8.3947 降至 5.3452，验证 Loss 从 8.3922 降至 5.3014，并保存为 `checkpoints/gpt_stage5.pt`；Stage 3 和 Stage 4 检查点均保留。

## 5.19 Stage 5 续训至 step 500

从 `checkpoints/gpt_stage5.pt` 的 step 100 继续训练至 step 500，保持 Batch 4、上下文 256、Embedding 256、8 个注意力头、6 层 Transformer 和学习率 `3e-4` 不变。训练 Loss 从 step 200 的 4.8291 继续降至 step 500 的 4.3426，验证 Loss 从 4.8481 降至 4.3749；最终训练/验证差距约 0.0322，尚未出现明显过拟合。

在 step 500 使用固定 10 条 prompt、每题最多 30 个字符、Temperature 0.8 和 Top-K 20 完成评测，结果保存为 `checkpoints/prompt_eval_stage5.json`。文本已开始出现较完整的小说片段，如“萧炎心中一眼起的一声”，但仍不能稳定回答问题，符合基础小说语言模型在早期训练阶段的表现。

## 5.20 Stage 5 续训至 step 5000

从 step 500 继续训练至 step 5000，每 500 步记录训练/验证 Loss，并使用固定 10 条 prompt、每题最多 30 个字符、Temperature 0.8、Top-K 20 生成阶段样本。step 5000 的20-Batch估计为训练 Loss 3.1407、验证 Loss 3.3964；使用100个 Batch 复核后为 3.1704/3.3448，差距约 0.1745。

模型已经能较稳定地产生小说式人物、动作、对话和标点结构，例如“萧炎沉默的道。一名老者微微点头”，但仍不具备问题理解或指令遵循能力。训练后期训练/验证差距开始扩大，后续需要增加按验证 Loss 保存最佳 checkpoint 的机制，避免只保存最后一步模型。step 5000 模型保存在 `checkpoints/gpt_stage5.pt`，每500步的固定 prompt 结果保存在 `checkpoints/prompt_eval_stage5_to5000.json`。

## 5.21 增加最佳验证模型自动保存

在训练脚本中加入独立的最佳 checkpoint 路径和保存逻辑：每次验证后将当前 Loss 与历史最佳值比较，只有取得更低验证 Loss 时才覆盖 `{prefix}_best.pt`；普通 `{prefix}.pt` 仍保存最后一步。检查点元信息新增 `best_val_loss`，日志分别记录最佳模型创建、改善覆盖、未改善跳过和保存失败，便于独立诊断。

新增 `tests/test_best_checkpoint.py`，覆盖首次创建、较差结果不覆盖和更好结果覆盖三条路径；项目全部 13 项测试通过，并用隔离的2步训练验证了真实文件与日志。由于旧流程没有保存 step 4000 权重，现以 step 5000 的100-Batch验证 Loss 3.344846 作为现存最佳基线，生成 `checkpoints/gpt_stage5_best.pt`；后续只有更低的验证 Loss 才会替换它。

## 5.22 Stage 5 续训至 step 10000

从 step 5000 继续训练至 step 10000，保持约 710 万参数的 Stage 5 结构、学习率 `3e-4`、每 500 步评估和固定 10 条 prompt 不变。最佳验证 Loss 依次在 step 5500、6000、7000、7500、8000、8500 和 10000 刷新；step 6500、9000、9500 未改善时，日志确认最佳 checkpoint 没有被覆盖。

step 10000 的20-Batch训练/验证 Loss 为 2.8335/3.0033，并将 `checkpoints/gpt_stage5_best.pt` 更新到 step 10000。随后使用100个 Batch 复核得到训练 Loss 2.8302、验证 Loss 3.0799、差距约 0.2496；差距表明模型开始更明显地拟合训练语料，但验证 Loss 仍较 step 5000 总体下降。最后模型保存在 `checkpoints/gpt_stage5.pt`，阶段样本保存在 `checkpoints/prompt_eval_stage5_5000_to10000.json`。

## 5.23 增加完整模型指标与 Loss 可视化

新增 `evaluate_stage5.py`，使用最佳 checkpoint 在100个验证 Batch、102400个字符预测上计算交叉熵、Top-1 和 Top-5 准确率，并对固定10条生成样本计算相邻字符、二元组和三元组重复率。正式结果为验证 Loss 3.0863、Top-1 42.71%、Top-5 62.86%；排除空格与换行后，三种平均重复率分别为 1.30%、2.59% 和 0.36%。

根据29个历史评估点生成 `reports/stage5_loss_curve.png` 与 SVG 版本，使用颜色和实线/虚线共同区分训练及验证曲线，标注 step 10000 最佳点，并提供 `stage5_loss_table.csv` 数据表和图表替代文本。评测、生成、数据与图表日志可分别过滤；新增5项指标及图表测试后，项目全部18项测试通过。

## 5.24 建立视频素材里程碑并确定 SFT v1 路线

建立 `VIDEO_MATERIALS.md` 作为项目视频素材总入口，规定每次实质优化都保留独立 checkpoint、配置、指标、固定样本、曲线、结论和 SHA-256。将 Stage 5 Step 10000 归档为 M001，模型复制为 `checkpoints/archive/gpt_stage5_pretrain_step10000_best.pt`，指标、Loss图表、CSV与每500步样本归档到 `reports/milestones/001_pretrain_stage5_step10000/`。

路线决定为停止继续堆叠当前字符级小说预训练步数，下一阶段直接在当前最佳模型上执行一次字符级 SFT v1，以固定问题展示预训练与指令微调的行为差异。该实验完成后再进入 BPE v2 路线，重新比较字符级与 BPE 模型的训练效率和 SFT 效果。

## 5.25 建立带原文证据的 SFT 种子数据集

建立 M002 字符级 SFT 数据集 v1，共 16 条可核验问答，其中训练集 12 条、验证集 4 条。每条问答都保存原文证据、原文行号和知识主题；准备脚本会逐条检查证据确实出现在授权语料的指定行中，并阻止同一主题同时进入训练集和验证集。

在原 4478 字符词表后增加 `<BOS>`、`<USER>`、`<ASSISTANT>`、`<EOS>`、`<PAD>` 五个完整特殊 Token，扩展词表为 4483。序列格式为 `<BOS><USER>问题<ASSISTANT>回答<EOS>`，问题部分标签设为 `-100`，仅回答和 `<EOS>` 参与交叉熵 Loss。新增分模块轮转日志与 5 项自动化测试，为下一阶段从 M001 checkpoint 开始 SFT 做好数据基础。

## 5.26 扩展为50条蒸馏式 SFT 试验数据

保留 M002 的16条种子集及哈希不变，另建 `sft_pilot50_v1`。数据包含40条训练、5条验证和5条测试问答，覆盖人物关系、炼药师、斗技、学院、异火与世界设定；测试记录独立导出，SFT 期间不得用于 Loss 或参数更新。

问答由教师模型依据授权原文整理，属于带原文约束的数据蒸馏。准备程序逐条验证证据与指定原文行完全对应、问题与答案字符均在词表内、切分数量正确、问题无重复且知识主题不跨集合泄漏。该批数据用于跑通 SFT 和制作训练前后对比，验证有效后再分批扩展到200至500条。

## 5.27 保存特殊 Token 扩展后的 SFT 前问答基线

从归档的 Stage 5 Step 10000 最佳预训练 checkpoint 初始化 SFT 模型，将词表由4478扩展到4483。原有 Embedding、Transformer 与输出层参数逐项复制并验证完全一致，只随机初始化5个特殊 Token 对应的2565个新参数；总参数由7097214增加到7099779。初始化 checkpoint 独立保存为 `checkpoints/archive/sft_stage1_init_pre_sft.pt`，其中 `sft_steps=0`，未计算训练 Loss、未反向传播、未更新参数。

使用5道隔离测试题、最多30字、Temperature 0.8、Top-K 20和固定随机种子记录 SFT 前完整输出。结果 Exact Match 为0/5、答案包含率为0/5、`<EOS>`主动停止率为0/5；所有回答仍表现为小说续写。结果归档在 M002b，后续 SFT 必须使用同一脚本与生成配置复测。

## 5.28 完成20步 SFT 安全试跑

使用40条训练、5条验证问答从 SFT 前初始化 checkpoint 训练20步，Batch Size 4、学习率 `1e-4`、Weight Decay `0.01`、梯度裁剪 `1.0`。训练脚本只加载训练和验证字段，5条固定测试题未进入训练或调参，报告记录 `test_records_consumed=0`。

完整训练 Loss 从4.4718降至1.9940，验证 Loss 从4.4909降至3.4976；第5、10、15、20步均刷新最佳验证模型。每一步均产生有限的梯度并完成裁剪与参数更新，证明回答部分标签、交叉熵、反向传播和 checkpoint 流程全部跑通。训练样本首次学会主动输出 `<EOS>`，但验证样本仍然无关，且训练/验证差距约1.50，说明小数据很容易被记忆，后续应谨慎增加步数并扩充数据。

## 5.29 将 SFT 数据分批扩充到100条

保留原50条试验数据和5道固定测试题完全不变，新增50条带原文证据的蒸馏式问答。组合数据现为80条训练、15条验证、5条测试，共100个互不重复的知识主题；新增内容覆盖修炼等级、炼药条件、魔核用途、魔兽等级、飞行斗技、紫云翼、玄重尺和焰分噬浪尺。

全部100条记录通过原文行证据、词表字符、ID与问题重复、切分数量和跨集合主题泄漏检查。最长序列50个 Token，屏蔽标签1528个，回答与 `<EOS>` 监督标签969个，扩展词表仍为4483。下一次训练将重新从 SFT 前初始化 checkpoint 开始，使数据量成为40条试跑与80条训练实验之间的主要变量。

## 5.30 将 SFT 指令数据扩充到500条

完整保留 M003b 的100条证据问答与5道固定事实测试题，新增400条从授权清洗语料确定性生成的原文补全指令。最终数据为400条训练、75条验证和25条测试；新增部分按320/60/20切分，并使用4种提示语各100条，训练模型识别指令、进入助手回答区和学习 `<EOS>` 停止。

新增400条分别来自400个独立原文行，证据均能在指定行精确找到，没有新增训练、验证、测试来源行交叉。校验同时发现旧100条在原文第378行存在一处历史训练/验证共同来源，本阶段为保持已有实验可比性而保留，并在报告中明确披露。500个问题和500个主题均唯一，词表仍为4483；全项目42项测试通过。

本阶段不把数量包装成质量：新增400条主要是原文补全指令，并非400条人工审核的事实问答。它适合做“数据量与指令格式”实验，但后续仍需补充更高质量的直接问答、解释型回答和拒答数据。下一次训练必须从同一份 SFT 前初始化 checkpoint 重新开始，避免把20步试跑的已有更新混入对照。

## 5.31 构造高质量1000条SFT数据集

建立 `sft_hq1000_v2`，保留原100条证据事实问答，新增900条干净概念分类问答，最终切分为800条训练、100条验证和100条测试。新增数据覆盖150个独立概念：46个人物、20种斗技或功法、15种异火、15种丹药、26个势力、18个地点和10种物品或体质。

每个概念生成5至7种问法，包括直接分类、完整类别判断、肯定回答、错误类别纠正、多项选择和一句话解释。同一概念的所有问法固定进入同一个集合，防止同义问题跨训练与验证集合泄漏。1000个问题全部唯一，新增150个概念分别绑定150个不同原文行，最长序列51个字符级Token，词表仍为4483。

构造过程中曾生成一版900条原文上下文题，标签虽可验证，但质量抽样发现OCR错字和截断句，因此该方案被淘汰并保存在里程碑目录作为失败材料，没有进入最终张量。最终数据不复制长篇原文上下文，避免把这些错误引入问题。全项目48项测试通过；下一步从同一个SFT前初始化checkpoint重新训练，先跑20步，再比较事实题、分类题和EOS停止率。

## 5.32 完成1000条数据的20步SFT安全试跑

训练脚本新增可配置的数据、初始化checkpoint、最佳/最终checkpoint、报告、阶段和里程碑路径，默认值保持旧实验兼容。本次明确加载800条训练和100条验证数据，从统一SFT前checkpoint训练20步；100条测试数据没有参与。

训练Loss由5.1175降至1.7259，验证Loss由5.2546降至2.0471，第5、10、15和20步均刷新最佳模型。两个监控回答都主动生成EOS，且checkpoint重新加载后元数据分别确认20步和测试参与数0。安全闭环通过后，正式训练仍从统一SFT前checkpoint重新开始，避免混入试跑更新。

## 5.33 完成1000条数据的800步SFT与正式评估

使用Batch 4、学习率 `1e-4`、Weight Decay `0.01`和梯度裁剪 `1.0`训练800步，约等于4个epoch。验证Loss在第300步达到最低1.2351；其后训练Loss继续由0.4354降到0.1295，但第800步验证Loss回升至1.2891，显示轻微过拟合。最佳第300步与最后第800步分别保存，没有互相覆盖。

模型选择完成后，第300步最佳模型一次性评估100条保留测试：Exact Match 26/100、修正后的答案包含26/100、EOS主动停止100/100。原5道事实题完全正确率0/5；固定10个开放问题显示SFT前模型以小说续写为主，SFT后虽然会短答和停止，却经常把任何问题回答成“人物”“势力”或“异火”。这暴露出900条分类变体压倒100条事实问答造成的分类偏置。

本阶段因此不把训练Loss下降包装成问答成功。结论是停止继续训练当前数据，下一轮减少重复分类题，增加事实、关系、解释、一般指令和不知道/拒答样本。训练报告、完整逐题评估、固定10题表格、Loss CSV、PNG/SVG曲线和审查结论均归档在M003f，供后续视频展示成功项与失败模式。

## 5.34 构造平衡版SFT v3数据集

为修复M003f的分类偏置，重新构造1000条SFT数据并保持800训练、100验证和100测试，便于与上一轮公平比较。新组成是200条原文事实问答、150条事实答案核验、150条概念身份、200条明确复制与比较指令、150条日常对话和150条资料不足回答；概念分类占比从上一版新增数据的100%降至全数据的15%。

数据包含350个独立主题和450种不同答案。200条事实问答来自100个知识主题的原始问法与直接回答改写，明确记录为200条监督样本而不是200个新知识；150条对话覆盖50个人工意图；150条未知题围绕50个经程序确认不在授权语料中的虚构实体。最高频答案“现有资料不足，无法确定。”占15%，已在质量报告中披露可能造成过度拒答。

构建审计逐条验证原文证据、正负事实标签、原样复制、相同与不同文本关系、未知实体缺席、词表字符和主题隔离。比较题修正了初版训练集只有“相同”标签的问题，最终训练为20/20、验证为3/2、测试为5/5。固定10题与数据精确重合为0，最长序列74 Token，词表仍为4483，测试集尚未使用。下一步必须从统一SFT前checkpoint先跑20步，不能接着旧SFT模型训练。

## 5.35 完成平衡版SFT v3的20步安全试跑

从统一SFT前checkpoint使用800条平衡训练数据更新20步，Batch 4、学习率 `1e-4`、Weight Decay `0.01`和梯度裁剪 `1.0`。训练Loss由5.6150降至3.2161，验证Loss由6.2076降至4.5047，第5、10、15和20步均刷新最佳模型；梯度、EOS、保存和重新加载正常，测试参与数为0。

## 5.36 完成平衡版SFT v3的800步训练与公平评估

正式训练再次从统一SFT前checkpoint开始，未接续安全试跑或旧分类模型。验证Loss在第400步达到最低3.4099，之后第500至800步逐渐回升到3.5379，而训练Loss继续下降到0.1630，因此选择第400步作为最佳模型并保留第800步作为过拟合对照。

使用同一套v3测试、相同Temperature、Top-K、长度和逐题随机种子比较旧分类版第300步和平衡版第400步：旧版Exact Match为0/100，平衡版为38/100，两者EOS停止均为100/100。平衡版分项为事实0/10、核验9/15、概念1/15、明确指令8/25、对话0/15、未知20/20。总体提升主要来自未知模板与较简单的核验任务，不能证明开放事实问答或日常聊天已经可用。

固定10题显示平衡版消除了“万物分类”的主要偏置，但答案仍会拼接不相关训练片段。至此字符级SFT v1教学实验完成：数据分布能显著改变行为，但继续增加Step无法克服字符级Token效率、710万参数容量和有限独立知识覆盖。下一阶段进入BPE Tokenizer路线，字符级模型作为完整对照组保留。

## 5.37 完成手写BPE、完整语料编码与10000步预训练

从原4478个字符Token起步，手写基于相邻频次和有序合并规则的BPE，不依赖现成Tokenizer库。从均匀分布的749952字符样本学习2000条合并规则，得到6478词表；再逐行编码完整6544278字符语料。最终Token数为4067842，减少37.84%，平均每Token表示1.609字符，全文解码与原文逐字符一致。

使用Block 256、Batch 4、Embedding 256、8头、6层和8123214参数从零训练10000步。验证Loss由第0步8.8201降至第10000步4.8036，最终步同时是最佳步；训练耗时2925.7秒。固定10题显示输出由随机乱码发展为局部连贯小说，但仍把问题当作续写开头，证明BPE改善表示效率，不会自动产生指令遵循。

## 5.38 完成BPE版平衡SFT与字符版公平评测

将同一1000条平衡SFT数据改用BPE编码，保持800/100/100切分和六类比例不变。扩展5个聊天特殊Token后，逐项确认全部预训练权重精确复制；20步冒烟测试通过，再从同一初始点正式训练800步。验证Loss在第400步达到最低3.7711，800步回升至3.9441，因此选择400步最佳并保留800步过拟合对照。

在模型选择后首次使用100条保留测试，并用同一问题、Temperature、Top-K、随机种子和字符输出上限比较：字符版精确38/100、包含41/100；BPE版精确36/100、包含42/100；两者EOS均100/100。BPE版在明确指令包含率上有小幅收益，但事实问答和日常对话仍为0，严格正确率没有超过字符版。结论是BPE成功解决Token效率和上下文覆盖问题，但当前回答质量主要受812万参数容量、独立知识覆盖和SFT数据质量限制。

## 5.39 制定单语料云训练容量方案

基于6544278字符、4067842个BPE Token和现有语料审计结果，区分了“显存能容纳的模型”与“单本小说能够支撑的模型”。取消原3211万参数正式方案，确定约824万参数为实用上限，并设计8层、Embedding 256、8头、上下文1024、权重共享的最大模型配置。

建立 `CLOUD_TRAINING_PLAN.md`，记录语料v4、3000条SFT v4、预训练/SFT步数、A10卡型、6～12小时运行窗口以及睡前一次验收后可无人值守的checkpoint、早停、GPU监控和自动归档要求。本阶段只完成方案和只读审计，尚未修改训练器或开始租卡。

## 5.40 冻结v4语料并生成带EOS的BPE张量

完成113组章节版本审核，修复29行可核验的异常标题，删除1段SHA锁定的跨章旧稿与14个非正文块；初始26个编号缺口最终核实为9个源文件无独立标题的缺口。按章节组生成90/5/5分片，只用train统计学习2000条BPE合并规则，并验证train/val/test逐字还原以及1599/92/84个EOS与章节数完全一致。

## 5.41 启动本机810万参数正式预训练

在M4 16GB上采用Context 512、Micro Batch 2和梯度累积4完成3步MPS冒烟及断点恢复测试，模型参数量为8105025。正式3000步训练已从随机权重启动，每100步验证并保存最佳/最新检查点，每500步保留固定10题的最多30字生成样本。

## 5.42 完成本机v4正式预训练并归档SFT前基线

正式训练在Apple M4的MPS上完成3000步，耗时2201.6秒。验证Loss从Step 100的7.2220下降到Step 2600的最低5.0447；最后Step 3000训练/验证Loss为4.9333/5.0767，因此按验证集选择Step 2600而不是最后一步。所选最佳模型在测试集20个固定Batch上的Loss为4.9958，checkpoint校验、重新加载和独立评估全部通过。

固定10题每500步保存一次，训练后模型已能生成局部小说叙述和对话，但仍把问题当作续写开头，不具备可靠问答能力。这一失败边界与全部60组阶段样本、最佳模型10题、Loss CSV、PNG/SVG曲线、语料和Token清单、运行配置及SHA-256已归档为M006，作为后续v4 SFT前基线。

## 5.43 低学习率续训至6000步并建立全局评测Harness

从M006由验证集选出的Step 2600权重初始化模型，不继承旧AdamW状态，以最大学习率`5e-5`、最低学习率`1e-5`、Micro Batch 2、梯度累积4续训到累计6000步。结构仍为Embedding 256、8层、8头、FFN 1024、Context 512和8105025参数。第二阶段耗时2111.8秒，两阶段累计4057.5秒、处理24576000个训练Token，约为训练集7.19遍。

最佳验证Loss在Step 6000降至4.7759，相比M006的5.0447降低0.2687（5.33%）；独立固定随机种子的20个Test Batch为4.7476。Checkpoint SHA-256校验与重新加载通过。

新增固定5个小说开头的评测Harness，不允许仅凭Loss宣布模型更好。Harness检查输出长度、中文比例、4-gram重复、最长单字连写和与训练语料的最长逐字重合；Validation Loss负责筛选，自动门槛可以否决，语义连贯性必须人工复核。独立重新加载的Step 6000发布候选虽然Loss最低，却出现连续12个“试”，因此自动状态为`REVIEW`；样本还存在语法不自然、人物关系错乱和情节跳跃。本阶段结论只能是“Loss继续改善”，不能宣布综合生成质量改善。参数、曲线、40组固定样本、门槛报告和哈希归档为M007。

另用Step 6000最佳权重重新运行天气、写作指令、作品事实、概括、日常表达和推测等固定10题，每题最多生成30字。10题均未被直接回答，仍被模型当成小说前缀续写；完整JSON已作为v4正式SFT前行为基线加入M007。

## 5.44 保守修复并审计3000条教师SFT候选

直接复用教师模型生成的3000条JSONL，在不修改下载目录原文件的前提下建立只读快照和v4候选。程序把9类原始任务重平衡为冻结的7类配额，按知识单元、声明章节和实际证据章节整体切成2400训练、300验证和300测试，并验证主题、章节与组跨集合泄漏均为0。

第二轮内容预审发现部分结构合法的自动改写仍不自然，因此继续修复：章节标题题改为直接绑定正式标题，复制题不再截断句子，一句话任务真正限制为一个完整句子，单条证据无法证明的“唯一/最多”题改为证据片段理解题。最终2999条记录绑定到冻结v4语料，证据覆盖率99.97%；全部3000条通过v4 BPE编码，最大序列149 Token，最常见答案占比1.20%。

随后又机械修复60条原因题语病，并将2条“还需半年才到二星斗尊”却回答为“已经二星”的记录改成诚实未知。双重验证仍只剩600条val/test真人审核门槛未通过。AI辅助预审将其分为22条先修复、250条核验全局或聚合断言、138条检查任务改写和190条低风险人工确认；训练集中另有124条证据风险和1593条语义或任务改写风险，只有683条没有自动风险标记。因此本阶段继续保持M008 `needs_review`，不宣布数据达标，也没有开始SFT训练。

继续处理22条评估集章节错绑时，确认其问答事实均有精确原文证据，问题来自教师数据沿用旧版章节编号。程序把问题、答案、章节号和章节标题统一重绑定到冻结v4语料，同时在`origin`中保留旧编号和原标题；同样规则覆盖全数据137条旧章节标注。重新验证后评估集已无自动阻塞项，分为255条全局/聚合断言核验、148条任务改写检查和197条低风险人工确认；训练集分为9条证据风险、1683条语义风险和708条无自动风险标记。

## 5.45 建立全书实体索引并核验评估集全局断言

重新检查255条全局标记后，发现38条复制或澄清任务只是被“从未”等关键词误报；修正规则并保留回归测试后，实际全局/聚合子集为217条，完整3000条数据总量没有变化。程序按语料真实叙事顺序索引1775个保留章节段、1614个唯一章节号，分别计算实体首次出现、两实体最早同章和每个保留章节版本中的字面名字次数。

217条结果中，78条章节或先后顺序与索引一致，56条目标人物名字计数在所有保留版本中并列最高；44条章节断言和2条计数断言发生冲突，4条先后结论正确但答案章节号过期，33条“主要人物/无法判断”必须人工判断。工具明确不写入人工批准；同章不等于同框，名字次数不等于戏份，别名、代词和隐含指代仍是限制。独立验证仍只失败于600条val/test真人审核门槛，因此尚未开始SFT。

## 5.46 修正正式章节口径并降级无法证明的首次同框任务

对异常记录做上下文抽查时发现，冻结v4语料的每个章节可能同时保留分隔符确认的正式标题和缩进的旧版内嵌标题。旧定位器把两者都当作边界，导致部分章节号和首次出现索引口径不一致。定位、审计和全书索引统一改用分隔符确认的正式标题，并排除正文中的内嵌标题行；全数据累计262条旧章节号完成重绑定。

原始数据中的271条“首次同框”都有局部证据同时包含两个人名，却不能据此证明它们是全书首次同场。为避免把不成立的全局断言用于SFT，全部改成由证据严格支持的局部共现、事实核验或“无法证明首次”任务。花宗首次字面出现和3条人物先后顺序也通过通用索引规则修正。最终172条真实全局/聚合断言中，79条首次或顺序与索引一致，58条名字计数得到所有版本支持，35条保留语义人审，自动冲突为0。完整数据仍为3000条，独立验证仍只卡在600条val/test真人审核，没有提前开始SFT。

## 5.47 将不可靠全局断言全部改为局部证据任务

继续抽查全书索引结果时发现，人物“云山”的所谓首次出现实际命中了“高耸入云山峰”。这证明连续字符命中不能代替人物识别，名字次数也不能代替戏份或章节主角；此前137条机器支持项不能直接作为SFT事实标签。

因此一次性重构首次出现、出场先后、首次同现、戏份最多、章节主角和“全书未说明”任务，只保留当前证据片段可直接验证的结论；异火榜排名和斗技等级从同一片段机械提取。共1067条候选采用局部证据改写，二次检查确认1067/1067的答案实体、排名或等级均能在同一条已验证证据中重新找到。重建后全局断言扫描从172条降为0条，3000条总量、2400/300/300切分、7类配额、2999/3000证据定位、最大149 Token和零泄漏均保持通过；全项目164项测试通过。

本次没有伪造人工结论。600条val/test仍全部未批准，其中406条需审核任务改写、194条为低风险确认；训练集另有9条证据风险和1678条语义/改写风险。正式SFT仍被`val_test_human_review`门槛阻止。

## 5.48 建立SFT v4本地真人审核页面

新增只监听`127.0.0.1`的浏览器审核工具，按406条任务改写和194条低风险记录排序显示问题、答案、证据、章节与风险原因。审核人可选择通过、修改后通过或拒绝；修改与拒绝必须写说明，普通通过不能悄悄夹带编辑。决定在每次点击后原子写入独立JSONL，教师原文件和3000条候选均保持不变。

每条决定绑定候选SHA-256并记录真实审核人和点击时间；候选变化后旧决定会拒绝加载。UI、决定保存和验证日志可独立调级、轮转，日志不写审核人、备注或问答正文。后端健康检查、真实600条队列、406/194筛选和浏览器页面均已验收；5项新增测试通过，全项目测试总数增至169项。当前真实审核进度为0/600，没有自动代签。

## 5.49 完成600条评估集的Codex AI审核

经用户明确授权，Codex以`Codex AI reviewer`身份完成300条验证集和300条测试集的全部审核。审核先运行证据状态、局部改写、原样复制和候选哈希检查，再按知识主题处理语义错误；最终521条直接通过、79条修改后通过、0条拒绝，覆盖34个需要纠正的知识主题。

79条修改不是格式润色而已，包含药老境界从“斗尊”纠正为“半圣”、萧厉从“斗师”纠正为“大斗师”、把“萧炎包围而进”改成“萧炎被洪家强者包围”，以及多处主客体、性别代词、模糊指代、截断问题和超出证据扩写。每条决定保存候选SHA-256、实际AI审核人、时间、说明以及修改后的问答，原始教师数据和候选文件不变。

审核页面现显示600/600、待审核0。项目仍明确区分“用户授权的AI审核”与“独立真人审核”：现有验证器仍报告`val_test_human_review`未满足，不把AI决定伪装成真人签字。下一步是决定采用AI审核作为本教学项目的数据准入，或额外安排独立真人对79条修改项和风险抽样签字，然后把决定合并成冻结SFT数据。

## 5.50 合并AI审核决定并生成training-ready SFT候选

新增`finalize_sft_v4_ai_review.py`，把600条AI审核决定合并到一份新的冻结候选副本，不覆盖原始教师数据和原始candidate。脚本会校验每条决定绑定的候选SHA-256，79条`modified_approved`只更新问题和答案，521条直接通过保持原文；所有变更另写sidecar，明确标注为Codex AI审核，不冒充独立真人签字。

合并后发现79条修改中有17组问题文本撞车，脚本对19条重复问句添加轻量任务角度前缀，并记录为冻结阶段去重。输出仍保持3000条、2400/300/300切分和99.97%证据覆盖；老质量门禁只剩`val_test_human_review`，这是有意保留的治理边界。

训练集证据风险进一步处理：8条`fuzzy_chapter_rebind_requires_review`经正式v4语料精确验证后标记为已核验；1条“第1200章出现的丹药是什么？”源证据章节漂移，无法安全重绑，因此从training-ready文件中排除。最终生成`data/sft/v4_teacher_repair/sft_v4_teacher_ai_training_ready.jsonl`共2999条，用于下一步SFT准备；报告归档为M008的`ai_review_freeze_report.json`。

## 5.51 完成v4 SFT张量编码与20步安全试跑

升级`prepare_bpe_sft.py`，使其兼容v4的`topic_id`字段、2399/300/300切分和已内置聊天特殊Token的v4 BPE tokenizer。基于2999条training-ready候选生成`data/cloud_v4/sft_v4_ai_training_ready_tensors.pt`，最长序列149 Token，平均60.25 Token，监督Token 59426，被mask Token 118262；Loss只监督`答案<EOS>`，不监督问题前缀。

新增`train_sft_v4.py`，从M007最佳预训练checkpoint `runs/pretrain_v4_m4_continue6000/best.pt`启动，使用8105025参数v4模型做20步SFT安全试跑。当前Codex工具环境中MPS不可用，因此本次使用CPU、Micro Batch 1、每5步评估、每次2个评估batch；验证Loss从7.5522降到5.0587，best/latest checkpoint均保存并带SHA-256校验。

20步后的两条监控样本仍明显不合格，分别输出“第一个不说的”和““嘭！””。结论是SFT链路已经打通，但模型还没有形成可靠回答能力；下一步应在可用MPS环境下执行正式SFT，并用固定10题、验证Loss和人工语义抽样共同验收。

## 5.52 完成v4 SFT 500步正式小跑

将`train_sft_v4.py`从安全试跑脚本升级为正式SFT入口，增加参数校验、固定10题监控、每100步样本留档和Loss CSV；同时新增`plot_sft_v4.py`生成PNG/SVG曲线。基于M007最佳预训练checkpoint和2999条training-ready SFT数据执行500步CPU可复现实验，模型参数量8105025，测试集0条参与。

验证Loss从step 0的6.7152降至step 500的1.5823，最佳验证Loss为step 400的1.3442。固定10题显示模型已明显学会短答和EOS停止，但事实准确性仍不稳定：会把药尘答成萧战、把第300章标题答错，也有“天火三玄变”和“正确”等局部成功样本；因此本阶段判定为SFT格式能力建立，不判定为问答质量达标。

## 5.53 构造混合聊天SFT并训练到2000步

M010样本暴露出新的数据偏置：模型虽然会短答，但非小说问题仍容易被硬套成章节号、人物名和数字模板。为此新增`build_sft_v4_mixed_chat.py`，在2999条小说SFT training-ready候选之外，构造3000条通用聊天样本，覆盖普通寒暄、基础算术、项目概念解释、实时能力边界、学习计划、指令改写、诚实未知和领域切换。最终数据合计5999条，切分为4799/600/600；编码后平均51.56 Token，监督Token 125019，测试集0条参与训练。

从M007最佳预训练checkpoint重新启动SFT，先完成100步和500步检查，再训练到2000步。2000步实验的验证Loss从6.7727降到1.6600，当前最佳点即step 2000；相比M010，模型对“今天天气怎么样？”不再直接输出小说章节，对“玄风石碑是谁？”能回答“现有资料不足，无法确定”，说明聊天格式和拒答边界开始建立。

本阶段仍不判定问答质量达标。固定样本中“一加一”等基础算术仍会答错，药尘等人物介绍会混乱，部分解释题出现截断和串词。M011结论是：混合聊天SFT有效缓解“全是数字/章节”的偏置，但当前800万参数模型还需要更高质量通用数据、更长训练或更大容量才能稳定聊天。

## 5.54 将混合聊天SFT推进到5000步并复核样本

沿用M011的5999条混合SFT数据，从同一M007预训练checkpoint重新训练到5000步，每500步评估和采样。10个验证batch口径下，验证Loss最低点出现在step 3500，为0.8788；最终step 5000训练Loss降到0.3656，验证Loss为1.6075，提示训练集已经被学得很熟。为减少小评估样本波动，又用100个验证batch复核step3500和step5000，结果分别为1.1292和0.9791，说明最后checkpoint并非简单退化。

样本层面仍暴露关键问题：低温下“今天天气怎么样？”能回答无法直接看到实时天气，“我应该如何安排今天的学习？”也能给出三段式建议；但“一加一”仍答错，萧炎、药尘、异火被过度回答为“资料不足”，证据判断会把“韩枫和紫研”答成“韩枫和韩枫”。因此本阶段进一步确认：只增加SFT步数不能解决可靠聊天，下一步应建立分类型评估并补强高质量数据，而不是继续盲目堆step。

## 5.55 建立M011分类型SFT诊断评估

新增30题公开诊断集，把SFT效果拆成小说人物、小说事实、证据判断、能力边界、基础数学和通用聊天6类，每类5题。评估脚本固定checkpoint、采样参数和随机种子，只生成最多30个Token，并明确记录本诊断不消耗隐藏测试集。

第一次规则过松时得到10/30，但复核发现“只输出一个是”也可能误判通过，因此收紧章节标题、能力边界主题和数学数字匹配规则。收紧后的step5000 latest低温评估为5/30，EOS为29/30；其中小说人物0/5、小说事实0/5、基础数学0/5、证据判断1/5、能力边界1/5、通用聊天3/5。

本阶段结论是：模型已经学到部分聊天口吻和停止行为，但没有学稳可验证事实、算术和证据实体匹配。后续应按类别补强v5 SFT数据，而不是用总Loss或少数好看的样本宣布模型会聊天。

## 5.56 构造v5定向补强SFT数据并完成100步安全试训

基于M011的5/30分类型诊断，新增`build_sft_v5_repair.py`，保留5999条混合聊天SFT数据，再添加2000条定向补强样本。补强配额为小说已知人物450条、小说事实锚点250条、证据实体匹配400条、基础数学400条、实时能力边界250条、项目概念解释250条；最终数据为7999条，切分为6399/800/800，测试集仍0条参与训练。

数据构造生成5440条候选，过滤416条重复问题和35条tokenizer无法覆盖“拟”的候选后，仍有4989条可用候选满足固定配额。编码后最长149 Token，平均48.71 Token，监督Token为157815，张量SHA-256为`9ba003074f133cfd3c1d343211e2d095875fcae44e3edc9b0ab9c84eb050dd0b`。

随后从M007预训练checkpoint在CPU上执行100步安全试训，验证Loss从6.3811降到5.1405，说明训练链路和新数据可学习；但30题诊断仍为5/30，EOS为22/30，样本出现重复“萧炎”和串题。结论是：v5数据准备和试训通过，尚未进入效果验收；下一步应跑2000步正式SFT并用同一分类型评估比较。

## 5.57 完成v5补强数据2000步正式SFT并收紧诊断规则

从同一M007预训练checkpoint重新启动v5 SFT到2000步，没有从100步安全试训续训。10个验证batch口径下，验证Loss从6.9488降到1.6750，最佳点为step2000；固定样本和Loss曲线均已归档。

评估时发现诊断规则仍需继续审核：模型输出“第3000章/第30000章的标题是《收场》”时，旧规则会因为命中“收场”而误判通过。已将第300章标题题收紧为必须同时包含“第300章”和“收场”，并补充回归测试。严格口径下，step2000 latest低温分类型诊断为6/30，EOS为24/30；小说人物2/5、小说事实1/5、证据判断1/5、能力边界1/5、基础数学0/5、通用聊天1/5。

结论是v5方向有轻微收益，但没有解决核心能力。数学仍完全不可靠，证据实体匹配仍会把“韩枫和紫研”答成“韩枫和韩枫”，通用聊天被“不要回答章节号”的负样本污染。下一步应先修补v5数据比例和模板，再决定是否继续长训。

## 5.58 构造v5.1去数学、零完整提示重合SFT并训练到累计5000步

M013从M011的5999条混合SFT重新清理数据，移除数学、数学话题、领域切换、续写/复述元任务、污染前缀及冻结评估提示重合项，再加入361条按语义组隔离的小说事实、证据判断、能力边界、概念解释、自然聊天和指令遵循修复样本。最终形成4676条数据，切分为3629/520/527；数学记录、冻结评估完整提示及其子串包装重合、修复语义组跨split泄漏均为0。同主题或同知识的不同问法有意保留，因此不把公开诊断称为语义零泄漏或完全盲测。

评估升级为无数学v2口径，用指令遵循替换数学类别，所有指标都强制EOS。复核同时发现旧公开30题有21题与M012数据精确重合，因此`test_records_consumed=0`不能再被当成公开诊断无泄漏的证明。M012 step2000在新口径下的greedy基线为4/30；M013从同一M007 checkpoint重新训练2000步后，验证Loss从6.8401降至3.2569、最佳为2.7157，严格评估为4/30、EOS 29/30。

由于2000步少于3629条train记录且Loss仍有下降空间，又从M013 step2000 latest追加3000步，形成累计5000步。追加阶段最佳抽样验证Loss为1.0625（本段step2500，累计约4500步），latest验证Loss为1.7516；latest严格评估为7/30、EOS 25/30，连续两次greedy复测答案、评分和EOS逐项一致。通用聊天达到3/5，小说事实与证据判断各2/5，但小说人物、能力边界和精确指令仍为0/5。因此本阶段只判定“数学与污染数据修复完成、部分行为改善”，不判定“综合问答修复成功”，下一轮不再盲目增加同一数据的step。评估和样本脚本还新增checkpoint/数据摘要兼容门禁，split或任务family不一致时会记录错误并中止。

## 5.59 修复已知人物拒答并建立v5.2可恢复训练Harness

删除520条会诱发裸拒答或元澄清的旧数据，加入360条核心人物、扩展人物、关系纠错和有证据未知样本，并把训练改为通用45%、核心人物35%、扩展事实15%、未知5%的分池无放回采样。v5.2.2训练1000步后，“小说萧炎是谁”和“小说药尘是谁”均能直接正确回答；最新版公开人物题4/5、隐藏实体题19/20、EOS 20/20，相比M013人物0/5和隐藏0/20显著改善。

训练器同时保存并恢复模型、optimizer、随机数和sampler状态，使用完整495条验证集并记录各池覆盖率；日志按数据、训练、验证、checkpoint和编排模块独立轮转。整体公开诊断仍只有10/30，“丹王是谁”仍失败，所以本阶段只判定实体路由修复成功；单本小说的最大效果路线确定为保留810万参数、受控续预训练、重新SFT并增加章节/实体检索。

## 5.60 完成Stage A分词器与模型容量实测

为验证而不是猜测单本小说适合的模型容量，新增隔离BPE输出、验证BPC、测试集封存和Stage A自动汇总/绘图。1000、2000、3000 merges分别用同一4M主体训练500步，3000 merges以5.8747验证BPC胜出；它把训练集压缩到3223207 Token，平均每Token承载1.7091字符。

固定3000 merges后，从随机权重分别训练4205161、8362025和14880745参数模型1000步，每组暴露4096000 Token。最佳验证BPC依次为5.4969、5.3820、5.3239；1488万参数取得最低值，但相对836万只改善约1.08%，训练用时从22.5分钟增至38.2分钟。全部选择过程未加载或评估test张量。

本轮将路线改为双分支：1488万用于最大质量正式训练，836万用于高频本机迭代。1000步样本仍不流畅，所以只完成“候选选型”，不宣布模型效果达标；1488万也是当前实测范围冠军，不冒充已证明的绝对容量上限。完整表格、曲线、样本边界、日志说明和ADR归档至M015。

## 5.61 完成1488万参数正式预训练

使用BPE 3000和14880745参数模型从随机权重训练6000步，每250步评估、生成固定样本并留存可恢复checkpoint；共暴露24576000个训练Token，用时约91.9分钟，test全程未加载。验证BPC从Step 250的5.9149降到Step 5750的3.7612，Step 6000原始最低为3.7532但改善不足0.01；独立Harness中Step 5750为`PASS`，Step 6000因短输出为`REVIEW`，因此选择Step 5750作为下一轮SFT初始化点。完整曲线、120条训练期样本、两个候选独立样本、SHA-256和日志说明归档至M016。

## 5.62 完成1488万模型的SFT数据准入审查

新增可复现的`audit_sft_readiness.py`，以正式BPE 3000逐条复算v5.2.2的结构、Token长度、重复、split隔离、证据来源、审核状态、回答模板和明显语义风险。4516条均可编码且无精确重复，但3479条train只有68789个监督Token；数据中2340条仍为pending、995条含元证据话术、1299条依赖风格前缀、1600条命中高重复答案，且多轮和128 Token以上问答均为0。

全量规则发现2条确定章节顺序错误和17条残缺标点答案；其中一条“第167章下一章=第162章”曾被旧AI决定误批为approved，证明不能继承历史批量审核作为充分语义验收。M017因此判定当前数据Needs revision，不直接编码训练；正式目标调整为8000至12000条高质量数据，中心方案10000条、30万至50万监督BPE Token，并补齐多轮、长上下文、中长回答、实体覆盖、自然语言任务和真正封存的未见事实评估。

## 5.63 构造一万条SFT v6并完成正式训练

从正式预训练train语料和本项目概念库构造10000条SFT v6，精确切分为8000/800/600公开诊断/600密封测试，覆盖7个维度、1000条多轮、2068条长证据、1597章、70个人物和256个概念。独立审计修复词表外字符、源文本噪声、规范化重复、证据跨集合重复和监督量不足后，最终达到0重复、0泄漏、0数学题、0不可编码、0超长、0记录风险；train监督量337900 BPE Token，密封测试始终未使用。

以1488万参数正式预训练Step5750为初始化点，先完成2步MPS冒烟，再从原预训练权重重新执行Batch4、2000步正式SFT，8000条train恰好无放回覆盖一轮。完整验证Loss从6.1353降至2.5940，公开诊断上Step2000优于Step1250（Loss 2.6331对2.8050，EOS 95.8%对87.5%，相似度0.304对0.233），因此选Step2000作为当前轮次模型；但完全匹配仍为0，实际回答存在错实体、无关模板和串题，本轮明确判定“训练完成但行为门未通过”，下一步先改数据分布而不是继续盲目堆Step。

## 5.64 冻结预训练、SFT与非目标能力的职责边界

项目用户正式决定：后续必须明确区分“预训练负责什么、SFT能补什么、哪些能力不该追求”。预训练负责中文与小说分布、叙事结构和续写底座，不以BPC下降冒充会聊天或事实正确；SFT负责问题作答、指令格式、EOS、多轮交互、核心人物和基于证据的有限任务，不以SFT Loss下降冒充语义泛化。

本项目不追求把单本小说、1488万参数模型训练成通用ChatGPT，不加入数学题，不要求模型掌握实时信息，也不要求参数权重精确记住全书章节顺序、首次出现、全局计数和复杂关系链。全书细节交给章节/实体检索，模型负责理解问题和组织有证据的回答。完整决策、验收规则和视频表述边界记录于`docs/adr/0003-pretraining-sft-capability-boundaries.md`。

## 5.65 完成纯预训练能力审计并冻结Step 5750

在不读取SFT数据和封存test的条件下，建立56条可复算语料探针，并用60个等距validation窗口、16条固定续写和12条高低频人物Cloze比较Step 250、5750和6000。Step 250到5750的正式Validation BPC从5.9149降至3.7612，固定窗口Top-1从7.81%升至24.17%，Step 5750空输出与机械退化均为0；Codex AI辅助复核流畅度2.3125/5、局部连贯度2.0625/5，按预先冻结门槛判定“形成可用于后训练的小说语言基座”，但只是边缘通过，不是成熟小说生成器。

Step 5750到6000的BPC只改善0.00808、Top-1只提高0.166个百分点，context-lift Cloze MRR下降0.0208，机械退化从0升到6.25%，AI文本评分也回落。因此正式确认“当前语料、学习率、重复曝光与1488万配置已进入实用平台期”，继续保留Step 5750作为垂直SFT初始化点。该结论不等于理论极限或语料绝对上限；后续停止盲目堆预训练Step，转向有证据的小说问答、片段理解、续写改写、自然交互和EOS训练，精确全书事实由检索层负责。完整材料归档于`reports/milestones/019_pretrain_capability_audit/README.md`。

## 5.66 冻结SFT v7数据并建立纯预训练基线

围绕小说核心事实、单段证据、多片段RAG、垂直聊天、小说表达和能力边界构建10,000条SFT v7，按8000/800/600 public/600 sealed切分并通过独立验证，正式清单SHA-256为`422c35fa130a3e6fc3f656019515fc7c1115616aa396712ea501751aeda1b9e9`。Train+Val与Public张量物理隔离，SHA-256分别为`81a0654150e8834cf4bd0ef30cbdb7975eac93fe5f1acf8208c715b0badfef47`和`bf28705327c416b7a8de37a913ca85b0a28ac5faa4839b7b412ce69817c90166`。纯预训练Step5750在600条public上的Loss为5.86808865、EOS为7.1667%、机械重复为85.5%，固定16题0题生成EOS且16题全部截断，因此只作为SFT基座，不作为聊天模型发布。

## 5.67 完成SFT v7两阶段20步安全试跑

从14,880,745参数、BPE词表7,465、Context 512的纯预训练Step5750重新初始化，在MPS上先运行10步核心/聊天/边界路由，再切换10步六维完整混合，两阶段采样、反向传播、验证和checkpoint链路均通过。Validation Loss从Step 0的5.897454降到Step 20的5.556195，public与sealed训练消耗均为0。Smoke不续接正式训练；M020正式候选、每500步公开评估、预训练保持和外部人工门仍为`pending`，当前不能写成发布完成。

## 5.68 完成SFT v7正式训练与全节点评估

从纯预训练Step5750重新初始化并完成2000步正式SFT，Validation Loss在Step0/500/1000/1500/2000依次为5.897454/4.080846/3.671394/3.481479/3.356181，训练阶段public与sealed消耗均为0。相同600条public上的Loss由基线5.868089降至3.294267，EOS由7.17%升至82.33%、截断由92.83%降至17.67%，但Step2000机械重复仍为42.17%。固定16题AI辅助基本通过在Step500/1000/1500/2000为0/0/1/0，证明Loss与停止改善没有转化成可靠语义回答。

## 5.69 判定M020没有合格发布候选

预训练保持BPC相对退化在Step500/1000/1500/2000为6.007%/8.995%/10.217%/12.084%，四个节点都只有13/16续写非空，因此完整保持门全部失败。Step1000仅保留为BPC未越线的保守诊断候选，Step1500仅保留为行为较优但BPC越门的研究候选，Step2000因固定题严重重复反弹到15/16而排除。最终汇总严格候选为空、必需产物缺失0、完整性错误0、`release_ready=false`，独立外部审核仍为`pending`且本轮不启封sealed test。

## 5.70 复算SFT v7语义密度

只以Train作为Stop/Go范围，确认六种固定答案开头占49.95%、18个核心事实裸问覆盖为0、核心监督相对密度为0.6188、证据高复制占66.01%。旧v7被判定为`needs_revision / high`；本轮不再用增加Step强化这个分布。

## 5.71 用64条Canary证明有限问答容量

从纯预训练Step5750独立初始化，使用64条Train问法和16条未见问法Dev/Selection完成容量探针。高LR在两组上达到严格答案100%/100%，证明1488万参数模型和assistant-only管线能够学习有限小说问答；但固定窗口BPC退化超过43%，小说续写只有9/16至10/16非空，因此只作容量证明，不是候选。

## 5.72 用正式预训练Train Token联合回放控制遗忘

实现`total_loss = assistant_only_sft_loss + replay_weight × pretrain_next_token_loss`，每组从Step5750重新初始化并运行400步；Replay只读取正式预训练Train Token，每组消费204800个目标Token，预训练validation/test与SFT public/sealed优化消费均为0。对比0.50/0.25/0.10后，0.25取得最优折中：严格答案87.50%/68.75%，BPC只退化2.33%，关系错误、问答截断和严重重复为0，但小说续写仍只有15/16非空。

## 5.73 区分逐字答案失败与命题错误

对0.25 Step400的13条严格失败进行Codex AI非盲命题复核：8条是父子逆关系或别名的语义等价表述，5条是真正的漏答或串题。调整后Train为61/64（95.31%）、Dev/Selection为14/16（87.50%），逐fact最低分别为7/8与1/2。原严格exact gate不追溯改判；同一checkpoint仍因小说续写15/16非空而不是候选。下一步重构3920条A0 Train，用结构化命题评分和SFT格式原生小说续写，再以0.25为中心比较回放权重。

## 5.74 完成M022 R1第5000步复核并停止不合格配方

R1从纯预训练Step5750独立初始化，以3920条A0 Train和正式预训练Train回放运行5000步；Development只用于teacher loss与模型选择，SFT Public/Sealed、预训练Validation/Test均未参与优化。Core命题从Step3500的128/192提高到Step5000的161/192，但固定窗口BPC相对退化在3500/4500/5000已为+6.52%/+6.71%/+6.75%，超过≤5%保留目标，故不续训到6000步、不发布。

同一复核还发现两类边界行为均为0/32：旧输入一面暴露可直接回答的核心命题，一面要求模型在无证据时拒答，形成明确的目标冲突。下一步不靠堆Step，而是另建R2证据条件化边界数据，显式约束“只能依据给出的材料回答”，并以更高0.35回放权重从相同预训练基座重新开始；决策证据保存于`reports/milestones/022_sft_v7_1_a0/r1/first_review_decision.md`。

## 5.75 完成M022 R2第1000步证据条件化复核并停止

R2保留A0核心与原生续写记录，只将边界任务改为“只能依据下方提供的证据材料作答”，并从同一纯预训练Step5750以`replay_weight=0.35`重新初始化。第1000步的固定窗口BPC相对退化为+3.72%，通过≤5%保持目标；Core只有23/192，Needs evidence为32/32，Grounded recovery与Insufficient evidence均为0/32，且固定16条小说续写有1条立即EOS（15/16非空），触发硬门。

首次评估把`证据不足：`错当作未拒答，发现后R2专用评分器已按冻结契约重跑，修正为Needs evidence 32/32；其余结论不变。因此不从Step1000续训到计划Step3000，不读取Public/Sealed，也不发布。R2说明“修复任务语义冲突”是必要条件但还不充分；下一步将先做分离核心、边界与原生续写影响的R3小规模消融。完整证据在`reports/milestones/022_sft_v7_1_a0/r2/first_review_decision.md`。

## 5.76 完成M022 R3监督家族消融

从同一Step5750基座分别只训练核心事实、证据边界、原生小说续写各250步，并补测R2同一步数的混合参考；每组均使用0.35正式预训练Train回放，Development、Public、Sealed和预训练Validation/Test均未参与优化。四组BPC退化均在+1.16%至+2.80%，但固定16条小说续写分别只有14/16、14/16、15/16和13/16非空，全部触发硬门。

边界单训快速达到Needs evidence 32/32，却不能完成充分证据恢复或干扰证据判断；核心单训只有1/192，说明250步不足以学习96条事实目录。混合组可同时学到两种拒答，但依然没有充分证据恢复。结论是不直接开始下一次长程混合训练，先建立R3b核心事实学习曲线；完整表在`reports/milestones/022_sft_v7_1_a0/r3/summary.md`。

## 5.77 完成M022 R3b核心事实学习曲线

R3b从同一纯预训练Step5750重新初始化，只优化1920条核心事实Train和0.35正式预训练Train回放；Development、Public、Sealed及预训练Validation/Test均未进入优化。预先冻结500/1000/1500/2000四个节点，Core由14/192升至56/192、84/192和95/192。第一个同时满足BPC≤5%与固定16条小说续写16/16非空的节点是Step1500（Core 84/192、BPC+4.63%），Step2000继续提高到95/192、BPC+4.82%。

该结果纠正了“核心单训无法学习”的错误推断：R3的250步只是不足一轮。另一方面，由于R3b没有训练证据边界，Needs evidence与Insufficient evidence在所有节点都为0/32，不能称为最终问答候选。后续应从R3b Step2000以短程节点化课程加入边界，并每个节点同时检查事实、充分证据恢复、拒答、BPC和16/16续写。一次首轮只读统计在Step500发现字段名兼容错误后即停止，失败文件保留；修复评估层后从基座重跑全部节点，完整证据在`reports/milestones/022_sft_v7_1_a0/r3b/summary.md`。

## 5.78 完成M022 SFT课程训练并冻结结论

R4只训练边界会迅速学到拒答格式，却将Core降至26/192并把BPC推到+6.45%；R4b随机交错和R5固定3:1可恢复部分共同能力，其中R5 Step1500为Core 97/192、Needs 28/32、Insufficient 27/32、续写16/16，但BPC+5.05%、Grounded recovery仅8/32。R6提高充分证据恢复抽样权重后，1000与1500两个连续节点的两类拒答均回落为0/32、充分恢复停在10/32，按冻结规则停止。

因此本轮SFT训练结束但没有发布候选，不启封Public/Sealed，也不把“部分拒答成功”包装为可靠问答。结论是当前模型与数据无法稳定完成证据给定后的事实恢复；下一阶段应先建立章节实体检索和gold-evidence评测，再重新定义检索增强SFT。完整对照在`reports/milestones/022_sft_v7_1_a0/sft_course_conclusion.md`。

## 5.79 R7证据充分补强与全参数课程闭环

R7 只从冻结 R2 Train 的 320 条充分证据记录派生 2560 条训练项，每条使用八种不同的“读证据→陈述事实”问答表达；开发集、Public、Sealed、预训练Validation/Test均未读取或进入优化。每次更新固定使用1份核心事实、2份R7充分证据、1份拒答边界，并混合84%正式预训练Train回放；2000步在500、1000、1500、2000四个节点均完成相同的开发与保持测量。

该路线未通过：最终Core为86/192，充分恢复5/32，缺少/不足证据为15/32与13/32，BPC退化+5.34%，小说续写15/16非空，四节点都触发保持硬门。它证明仅增加证据充分表达数量并提高采样权重会破坏拒答与续写，不能把训练Loss下降等同为证据问答能力。R7及此前R3b–R6共同构成当前全参数SFT阶段的闭环反例；没有产生发布候选，也没有启封任何盲测。详见`reports/milestones/022_sft_v7_1_a0/sft_course_conclusion.md`。

## 5.80 R8冻结底座LoRA课程

R8 从正式预训练Step5750开始，冻结原始14,880,745参数，在40个注意力和前馈投影上安装rank16 LoRA，仅训练819,200个增量参数；安装时输出与原基座严格一致。它继续使用核心/边界3:1和正式预训练Train回放，在500、1000、1500、2000步做同一套开发与保持复核。

R8解释了另一侧取舍：Step1500的BPC为+4.98%、小说续写16/16，保持门通过，但Core只有26/192、充分恢复2/32、干扰证据0/32；Step2000虽Core升至29/192、充分恢复3/32，BPC已到+5.09%。结论不是“LoRA无效”，而是它对当前底座的遗忘控制有效、对96个事实和三态证据任务的适配容量不足。R8不能发布；下一步必须建立检索增强任务，而不是继续改相同SFT配方。

## 5.81 完成M023检索增强SFT课程

M023 将事实定位和语言生成拆开：96张审核事实卡只从正式预训练Train短窗口中检索证据，冻结Development的288条事实问法达到Top-1 288/288、Recall@3 288/288。阅读器数据为Train/Development 3920/392；优化只使用Train与正式预训练Train回放，Development只前向评估，Public、Sealed和预训练Test正文读取为0。

R2完整运行到2000步，Core 26/192、充分证据恢复5/32、两种拒答均32/32、BPC+2.65%、续写16/16。R3为排除正例证据曝光不足，从同一Step5750重新训练3000步，每次更新使用3份正例、1份拒答与预训练回放，使每个正例约有8次暴露；最终Core提升到57/192（29.69%）、充分证据恢复10/32（31.25%）、两种拒答均32/32、BPC+3.17%、续写16/16。保持目标全部通过，但未达到Core≥70%和充分证据恢复≥75%的严格发布门。

因此M023的SFT课程已经完成而没有发布候选，不启封盲测。结论不是“检索无效”：路由层可靠解决了“到哪里找证据”，拒答边界也已稳定；瓶颈在当前1488万参数小说底座无法可靠执行“读短证据并恢复事实”。下一阶段若继续，应先做架构选择（更充分预训练/更大底座，或确定性抽取式回答器），不能把重复同类SFT视为自动进步。完整指标和哈希路径见`reports/milestones/023_retrieval_sft/sft_course_conclusion.md`。

## 5.82 M023 R4结构化事实卡安全检查点

R4不改变事实目录、路由、开发集、评分器、基座或保持评估，只把直接证据的输入表示从原文短窗口改为同一审核事实卡的结构化检索表示。它的假设是：若当前瓶颈主要是小模型对原文语义归纳困难，透明卡片应改善“已给证据→回答”；该结果只能称为结构化检索卡条件化生成，不能称为原文阅读能力。

该实验从Step5750重新初始化，使用3920/392 Train/Development、3:1正例/拒答和0.65正式预训练Train回放；Public、Sealed、预训练Validation/Test正文读取均为0。预注册节点为500、1000、2000、3000；在新实验治理要求下第2000步完成完整测量后不自动续训。Core从R3同一步的45/192提升至113/192，充分证据恢复从8/32提升至21/32；两类拒答各32/32、BPC+3.13%，但固定小说续写仍为15/16非空。因为Core、充分恢复和续写三门未通过，R4没有候选、不启封盲测、不发布。完整契约和停止记录见`reports/milestones/023_retrieval_sft/structured_fact_card_course/rerun4/safety_checkpoint_audit.md`。

## 5.83 R5后检查点尾训契约与只读预检

R5在启动前发现一个关键可复现性限制：R4 Step2000 checkpoint保存了模型、优化器和一个采样器状态，但没有保存所有独立采样器的状态。因此，新的1,000步轨迹不能诚实地描述为“只多训练了1,000步”。项目将唯一变量修正为冻结的`post_checkpoint_tail_treatment`：加载R4 Step2000模型权重，以固定新种子重新创建采样器与AdamW状态，再运行固定1,000步学习率尾程；这不是隐式恢复中断进程。

运行器默认只做预检，必须在用户确认实验契约后传入显式执行开关才会创建run。实际预检已校验父checkpoint SHA、结构化manifest和tensor SHA、Tokenizer、正式Train回放及3920/392数据族计数；预检不创建训练目录，优化、Public、Sealed和预训练Validation/Test读取均为0。第500步只要不触发预先登记的BPC、机械退化或“无共同进步且续写失败”停止门，才继续到预先登记的第1000步终点。契约和配置见`docs/experiments/m023_r5_structured_tail_contract.md`与`configs/m023_retrieval_reader_course_r5.json`。

## 5.84 完成R5后检查点尾训并拒绝延长

R5以R4 Step2000模型权重为父节点，重新创建固定采样器和AdamW状态，在500/1000步进行冻结Development、预训练保持和固定续写测量。第500步为Core123/192、充分恢复23/32、两类拒答32/32、BPC+3.31%、续写16/16、机械退化0；相比R4，核心与充分恢复均提高超过5个百分点且修复了空续写，因此按预注册规则继续到固定终点。

第1000步为Core129/192、充分恢复22/32、两类拒答32/32、BPC+3.43%、续写16/16、机械退化0。BPC、拒答和续写均通过，但Core仍低于70%（135/192）、充分恢复低于75%（24/32）；并且充分恢复在500到1000步回落。R5结论为`complete_no_candidate`：不以训练Loss选择、不再追加步数、不启封Sealed、不发布权重。完整聚合决定见`reports/milestones/023_retrieval_sft/structured_fact_card_tail_r5/r5_tail_decision.md`。

## 5.85 R6分层充分证据采样安全停止

R6以R5 Tail1000为冻结父节点，保持数据、模型、回放、开发集、评分和候选门不变，只把每次更新改为核心×2、充分证据×1、拒答×1。Tail500时Core升至139/192、两类拒答仍各32/32、BPC+3.55%、续写16/16，但充分证据恢复仅23/32，低于预注册的24/32安全线，因此不进入Tail1000。

9个充分证据失败项全是缺少所需命题或表面槽位，没有矛盾、错误拒答前缀或人工复核项。这说明下一步若继续，应该单独检验“给定结构化事实卡如何显式渲染为答案”，不能再用增加训练步数解释或掩盖失败。完整聚合决定见`reports/milestones/023_retrieval_sft/stratified_grounded_tail_r6/r6_stratified_tail_decision.md`。

## 5.86 R7显式答案槽位通过自动门

R7从R6 Tail500权重开始，保持事实目录、训练/Development切分、采样、回放、评分、步数和候选门不变；唯一变量是把同一结构化事实卡写成带`【已核验答案】`的字段，并要求答案完整保留人物、关系、数量和限定语。新的私有派生数据仍为3920/392、最大252/512 Token，Development优化记录和Public/Sealed/预训练Validation/Test正文读取均为0。

Tail250与预登记终点Tail500均为Core150/192、充分恢复24/32、两类拒答各32/32、BPC+3.67%、续写16/16；终点机械退化为0，自动门全部通过。这个结果只代表显式答案字段条件化生成的提升，不代表原文阅读或闭卷问答；SFT更新在此结束，后续需要独立真人审核，然后才可能一次性评估Sealed。完整聚合决定见`reports/milestones/023_retrieval_sft/answer_slot_tail_r7/r7_answer_slot_decision.md`。

## 5.87 建立R7独立真人审核入口

为终点Step4000准备64条固定分层审核包：核心16、充分恢复32、两类拒答各4、续写8。审核页面只监听本机`127.0.0.1:8766`，将每条通过、拒绝或需修订决定原子写入Git忽略目录，并将决定绑定不可变候选哈希；日志按UI、数据、校验、工件和编排模块独立轮转，且不记录正文、审核人或备注。

该工具不能自动批准、不能打开Sealed、不能修改训练数据或权重。它把SFT从“自动门已通过”推进到可由真人完成独立验收的状态；人工审核未完成前，模型仍不可发布。

## 5.88 完成R7真人审核并否决候选

64条审核全部完成：41条通过、16条需修订、7条拒绝；核心13/16、充分恢复27/32、两类无证据问答合计1/8、续写0/8通过。该结果推翻了“自动16/16非空即可代表续写通过”的宽松解释，R7正式关闭为`complete_no_candidate`，没有继续训练、启封Sealed或发布权重。

## 5.89 完成M024四任务意图只读对照

冻结有证据问答、无证据问答、小说续写、领域聊天各32条，以同一R7 Step4000在普通提示和隐藏任务标签下完成256次贪心生成；两次复跑得到相同预测SHA-256。路由混淆代理率从60.2%升至64.8%，有证据问答代理通过率从84.4%降至81.2%，因此“推理时只加任务标签”假设不成立；全程没有优化器、反向传播、权重变化或Sealed/预训练Validation/Test读取。

## 5.90 加固M024评分与复现绑定

独立代码审查发现旧评分可让领域聊天同时出现`proxy_pass=true`和`route_confusion_proxy=true`。本轮将两者改为强制互斥并在聚合层失败关闭，收紧问候、能力说明和意图澄清代理，同时把矛盾的“证据充分+资料不足”判为无证据问答失败；修正后领域聊天自动代理通过率为普通提示2/32、隐藏标签3/32，不再把污染回答算作通过。

配置、生成策略、任务标签、Harness/评分实现、Tokenizer、基础模型配置、Python/PyTorch版本、实际设备和评分依赖均已写入哈希绑定。连续两次CPU只读复跑得到相同预测SHA-256 `e3bc932d2a241fa4722cf941d866675be2e81dedcbf454da0372438497ce6af1`及评分SHA-256 `ef91666a9c59c6a9df0af4a104cb96af12e1f3c0d033f2aeb43820917cf4cef8`；核心假设结论不变，仍不授权训练或候选选择。

## 5.91 建立M024匿名成对语义审核

为128个冻结pair建立本机A/B盲评入口，每对同时审核两份生成，共256项。浏览器只看到用户输入、参考行为、匿名回答A/B和六维语义rubric，不看到真实实验条件、内部任务标签或自动评分，也不填写身份；“需修订”只生成原因占位，不再自动粘贴参考答案。

每对审核以单行原子写入Git忽略目录，绑定records、predictions、scores、三份元数据、rubric、私有blind-salt哈希、pair候选、来源和并发revision的SHA-256。A/B顺序由`0600`私有salt做HMAC后在四个任务内各16/16平衡位置，浏览器只得到每次进程随机生成的opaque handle，不能从页面ID反推出真实条件。服务只监听`127.0.0.1`，会话令牌只经URL fragment引导且不进入HTTP响应/日志，并执行Host/Origin/Content-Type校验；单实例锁、磁盘SHA检查和非daemon请求收口防止并发覆盖。入口建立时审核进度为0/128对，本步骤没有训练、权重更新、Sealed读取或候选资格变化。

## 5.92 GPT-5.6教师完成123对盲审并留5对给用户

在读取回答前，按稳定ID哈希预先冻结5对真人留出：有证据问答1、无证据问答1、续写2、领域聊天1。其余123对拆成三个各41对的盲包，由`gpt-5.6-sol`以`high`推理强度逐题审核。教师看不到真实实验条件、自动分数、内部记录ID或留出映射；审核来源写为`ai_teacher_gpt_5_6`，没有冒充真人。

三包共123对/246项通过schema、唯一ID、固定包SHA-256、值域、严格通过条件和留出不重叠校验后一次原子导入。教师原始结果为通过105、需修订12、拒绝129；小说续写60项全部拒绝，领域聊天60项拒绝、2项需修订，严重跨任务污染114项。这个结果把用户“模型分不清问答、聊天、续写”的体感转化为冻结证据，也说明推理时单加隐藏标签没有解决根因。

只保留5对/10项给用户在本机UI体验。AI教师审核属于开发诊断，不能替代独立真人签字；本轮没有训练、反向传播、权重更新、Sealed读取或候选资格，`training_authorized=false`。

用户体验首题时发现完整训练式提示把任务规则、证据和问题连成一段，难以识别真正输入。审核页随后改为默认显示纯“用户问题”，单列“检索证据”，把“审核参考答案”明确标成非模型输出，并将完整原始输入折叠保留；A/B标题同步改为“模型输出A/B”。解析和匿名接口回归测试通过。

用户再次指出项目早已冻结“回答不要有证据充分固定前缀”，而教师rubric遗漏了该产品规则。复核123对发现145/246项输出含该固定前缀，49项曾被教师判为通过。公开报告增加不改写原始行的确定性自然风格策略层，将有效结果纠正为56通过、61需修订、129拒绝；审核页展示自然目标、折叠旧R7参考，违规输出不能再保存为通过。这个修正证明评测遗漏需求会直接制造虚高指标。

## 5.93 完成M024全部盲审并保留来源边界

用户在本机UI亲自完成原5对体验留出中的2对，随后明确委托GPT-5.6教师完成余下3对。委托包继续隐藏真实实验条件、内部ID和自动分数；3对/6项输出全部判为拒绝，结果经schema、固定包哈希、唯一ID、值域与严格通过约束校验后一次原子导入。私有包、映射、审核结果和最终决策文件均为`0600`并由Git忽略。

最终128对/256项全部完成，来源为`ai_teacher_gpt_5_6` 126对、`human_ui` 2对。全部原始判断为105通过、14需修订、137拒绝；自然回答策略共识别152项固定“证据充分：”前缀，49项原始通过被确定性降为需修订，最终有效判断为56通过、63需修订、137拒绝。原始审核行和模型输出未改写；独立真人签字仍未满足，`training_authorized=false`，没有训练、反向传播、权重更新、Sealed读取或候选资格变化。

## 5.94 完成M025任务条件化Smoke并校准安全门

两臂从纯预训练Step5750独立运行20步，保持相同自然目标、样本顺序、0.65回放和评测，唯一变量是训练与原生推理输入是否含既有BPE任务标记；两臂BPC均过门、续写32/32非空且标记泄漏为0，但机械退化均为28/32，按冻结契约停止，未进入正式500步或Sealed。随后用未经SFT的Step5750做同提示同解码校准，无标记/有标记输入已分别达到30/32与28/32机械退化，证明绝对25%门槛未按基座校准，不能把本轮失败归因于20步SFT。原`smoke_safety_failed`结论不追溯改写，下一步先冻结新的解码与相对退化契约，再决定是否重跑Smoke。

## 5.95 完成M026解码校准并重建M027 Harness

在不训练、不改权重的条件下，依次比较贪心、`temperature=0.8/top_k=20`和最后一次`temperature=1.0/top_k=20`；第三种策略使纯预训练基线的机械退化从30/32降为0/32，但旧32题集合的门槛定义存在冲突，因此没有追溯改判。随后从正式预训练Train张量冻结64个不重叠窗口，重建相对退化Harness；Step5750的普通输入和任务条件输入均为64/64非空、0/64机械退化，且标记泄漏为0，自动机械门通过，语义方向仍需一次独立真人校准。

## 5.96 冻结M027源隔离数据、运行器与五题人工门

M027 Train固定为2,284条：有证据问答640、无证据问答364、小说续写640、领域聊天640；与128条Development的源语义组、事实ID、精确及规范化提示/答案重叠均为0。两臂固定相同Step5750父权重、目标、样本顺序、四任务等量抽样、0.65回放和解码器，唯一变量是输入是否含12 Token任务标记；25项运行器/finalizer测试和两臂只读预检均通过，Smoke各计划20步、160次SFT抽样与80个回放窗口，受限数据读取为0。

训练前的五组匿名真人校准是失败关闭的必要门：需要完成10个输出的机械重复判断、5个pair的指标方向判断及“AI未代填”自声明。当前人工进度为0/5；显式执行命令已验证只会返回`human_calibration_pending`，不会创建run目录或启动优化器。

## 5.97 M027H真人校准未过门并保持训练关闭

五个匿名pair、十个输出已由真人完整判断并明确冻结，私有decisions和自声明均为`0600`且未收集身份。指标方向判断为5/5一致、自然顺接为9/10，但真人与冻结自动机械标签只一致7/10，低于预注册8/10；因此状态为`human_calibration_complete_gate_failed`，M027两臂Smoke未启动、optimizer step为0、Sealed/Test读取为0。

该结果说明当前解码整体可顺接，但二元机械Harness仍会漏掉真人感知的重复。项目不会事后把8/10降为7/10，也不会用同一10条反复调阈值；下一步是把这10条仅作为设计集归因漏检机制，并在全新提示集合上独立验证一个版本化的指标修复。

## 5.98 冻结M028零训练指标验证包

M028保持Step5750、`temperature=1.0/top_k=20`和输出长度不变，只修复机械重复测量；64个全新来源隔离pair、primary/reserve顺序、自动预测、匿名审核包与工件哈希均在外部判定前冻结。相关准备与finalizer回归测试共33项通过，optimizer step、反向传播、checkpoint写入、Sealed/Test读取均为0。

用户已完成人工体验五题并明确不再追加人工审核，因此原计划的32对独立真人验证没有执行，也没有被AI结果冒充为真人签字。项目另立M028A，仅把三次隔离的GPT-5.6教师判断当作诊断性代理标签；该代理即使通过也不能直接授权正式M029训练。

## 5.99 M028A三教师代理判定失败并关闭训练

三次独立上下文的`gpt-5.6-sol/high`分别审核同一组37个匿名pair（5个已知人工锚点加32个M028 primary），每次使用不同冻结乱序与A/B交换，且互相看不到结果。锚点多数总分为8/10，但特异度仅5/7；三教师的机械标签Fleiss κ为0.5032、方向标签κ为0.4210，均低于预注册0.60，说明代理裁判面板本身没有建立稳定ground truth。

在三教师多数票上，M028 v1指标为TP10/FN17/TN36/FP1：敏感度0.3704、特异度0.9730、平衡准确率0.6717，pair方向准确率23/32（0.7188）。它表现得过度保守，漏掉17/27个教师多数认定的退化输出；除特异度外均未达到0.80门。最终状态冻结为`m028a_ai_proxy_failed_evaluation_blocked`，没有启动M027/M029或任何后续SFT训练，也不宣称SFT已经不存在理论提升空间。

## 5.100 完成M030最终冻结与项目收尾

M030没有训练、反向传播或写入新checkpoint，只执行最终身份、隐私、运行态与发布边界的只读验证。审计确认没有训练进程，三个已经完成的本地审核服务已停止，8767–8769不再监听；预训练Test与SFT Sealed继续未启封、未评估。

项目正式冻结M016的14,880,745参数、BPE 3000纯预训练Step5750作为私有教学小说语言基座。所有SFT路线均无发布候选：M023 R7 Step4000的自动门结论已被41/64通过、续写0/8通过的真人审核否决，M028A又因教师一致性和指标敏感度不足保持评测阻断，正式M029没有启动。当前代码、语料与衍生权重许可尚未形成可公开核验链路，因此仓库和模型不可公开发布；终局也不允许被改写为“小模型或SFT的理论空间已经耗尽”。完整冻结事实、复现入口与产物索引见`reports/milestones/030_final_freeze/README.md`；任何未来续作必须另立新项目合同。

## 5.101 冻结纯续写SFT数据与实验合同

M031另立实验合同，只训练“给定小说片段后继续写”的单一行为，不混入问答、聊天、数学或检索任务。数据按章节隔离为1040条训练、64条开发和64条最终留出；正式优化器只读取训练集，预训练Test与历史SFT Sealed读取均为0。

## 5.102 发现全权重EOS导致提前停止

M031从正式Step5750基座重新开始训练200步，开发Loss由4.8283降到4.5263，但61/64条输出命中EOS、22/64条少于24 Token，形成提前停止坍塌。该checkpoint被拒绝，说明Loss下降不能替代生成行为验收。

## 5.103 在冒烟阶段叫停M032

M032只把EOS损失权重从1.0降到0.25，其余数据、基座和优化设置保持不变。20步冒烟测试仍出现1条空输出，因此按预注册门槛停止，没有启动正式训练，也没有读取最终留出集。

## 5.104 完成无EOS损失的纯续写SFT

M033将EOS损失权重设为0，改由最大Token或字符数控制推理停止；20步冒烟测试通过后，从Step5750重新训练200步并覆盖全部1040条训练记录。开发集与最终留出集的自动安全门均通过，64/64条留出输出非空且没有提前停止，证明模型学会了纯续写接口和可控输出长度。

## 5.105 语义盲评否决M033候选

GPT-5.6辅助教师对64组基座/候选匿名续写进行盲评：基座胜31、候选胜25、平8，综合分1.5313对1.4375，严重矛盾42对45。M033训练已经完成，但没有证明语义续写质量提升，因此实验权重仅保留作教学对照，正式模型仍是纯预训练Step5750；不会继续在同一数据上盲目追加训练步数。

## 5.106 M034验证更长SFT上下文

M034以正式Step5750为共同基座，在目标续写、数据切分、优化器和200步训练预算一致的前提下，比较`short64`与`long320`两种SFT可见上下文。两臂自动安全门都通过；GPT-5.6辅助三方盲评中，long320以37/22/5优于short64，却以25/37/2负于Step5750，且改善幅度未达预注册门槛。因此M034以`complete_no_candidate`停止，Final Holdout读取0条，正式模型不变。

## 5.107 M035冻结长上下文继续预训练A/B

M035把假设从“SFT时看更长”下沉到“预训练时看更长”。两臂共用同一个扩展到1024位置的初始权重，模型参数为15,044,585；`short512`每步使用2个512 Token序列，`long1024`每步使用1个1024 Token序列，两臂每步都预测同样的4096个目标Token，训练顺序、优化器、学习率和总暴露量绑定。

## 5.108 完成M035两臂1000步正式训练

`short512`与`long1024`均完成1000步，每臂暴露4,096,000个目标Token；本机耗时约为565秒和1033秒。终点checkpoint SHA-256分别为`ffb48013f2a903b812d3bcc8482a96b67ca313e093d764411a9b0015f560d88d`与`1cbae9320d1beccbbb98d3e8f5bde7ea90159faec9d761e5b032c6069ef8aff6`。训练期间预训练Test、历史SFT Public/Sealed及最终Holdout正文读取均为0；评测未结束前，这两个权重都只是实验臂，不是新的正式模型。

## 5.109 M035.1在看到模型结果前修正评测窗口

执行前检查发现旧Development和Formal-Validation Holdout中有少量记录不能为512与1024两种可见上下文同时提供足够边界。M035.1因此只修正评测窗口，不改训练数据、权重、门槛或选择规则；修正后Development 64/64、Holdout 32/32都满足可见性要求，最小边界为4 Token。旧工件保留作审计证据，正式评测只允许使用冻结的v2工件。
