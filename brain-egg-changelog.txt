# 📓 改动记事本（从 v1.0 开始累计）
> 位置：~/Documents/trae_projects/game/改动记事本.md
> 维护方式：TRAE全局 PreToolUse Hook 每次 Edit/Write/DeleteFile 自动追加
> 版本号：vX.Y，跨天自动升 X（主版本号），每次改动自动升 Y（次版本号）
> 人工大改动也可以手动追加成 ## vX.Y（格式：## vX.Y — YYYY-MM-DD）

---
## Web / 微信小游戏 v190 第 5、12、15 关交互修复 — 2026-09-01

- 第 5 关敲门改用新生成的原创木质敲击采样 `assets/sfx-door-knock.wav`，Web 与微信小游戏共用同一 PCM16、单声道、16 kHz 资源；微信适配层通过 `door-knock` 样本标识走专用资源路由，不再使用像电子短音的合成音。音频创建或播放失败仍只记入 `audio` / `audio-core`，不会影响物理帧。
- 第 12 关根因是达到 720 像素拖动阈值时，旧代码把门从视野外直接重设到 `player.x + foundDistance`，因此会闪到屏幕中间。v190 移除该传送：关卡载入复位阶段就把门锚定在当前镜头右边缘之外，超宽微信屏自动换关的首帧也不会先画出原始门；达标时只设置保留完整拖地偏移的世界目标，实际门以每秒 840 像素上限逐帧追赶；即使真机把多个触摸移动合并成一次大跨度事件，也会先从右边缘自然进入。
- `mechanic:world/world-door-found` 新增初始/目标屏幕位置、`entrySpeed` 和 `revealMode:right-edge-animated`，追到目标后另记 `world-door-entry-complete`；可只开启 `mechanic:world` 判断门的入场轨迹。走路仍不能发现门，只有从真实地面起手的拖动累计有效。
- 第 15 关根因是旧碰撞只在特定横向越界条件下处理侧面，且没有墙体底面/顶面碰撞，所以大步长或跳跃可能直接穿过。v190 新增 `solidVerticalBlock` 纯逻辑辅助，并强化预先重叠恢复：正常人物不可走穿或从下方跳穿，可从上方落到墙顶；缩小后的碰撞体仍可从通道通过。
- 第 15 关水平和垂直阻挡分别通过 `collide/shrink-tunnel-side-block`、`collide/shrink-tunnel-vertical-block` 记录；日志包含阻挡方向和墙体几何，不记录用户数据，也不逐帧重复刷屏。
- 正式主线保持 20 关，“创造秘境”继续下线且历史数据保留；背景音乐、宽屏真实地图、自动下一关与其他关卡逻辑不变。v190 加入第 12 关多宽度右侧入场、第 15 关侧面/底面/顶面碰撞、第 5 关独立音效资源与微信路由的自动化回归。
- 2026-09-02 已通过微信开发者工具官方 CLI 将同一份代码上传为平台版本 `1.0.190`；上传与预览包均为 824,395 字节。新真机测试二维码保存为 `/Users/bytedance/Documents/ChatGPT/game/wechat-test-qr-v190.jpg`（470×470 JPEG，SHA-256：`4e5f42aee555dbed3205ce1f282c3d95be12504ffe5c04daec66eb4fcf5ab005`），预览与上传诊断分别保存为 `wechat-preview-v190.json`、`wechat-upload-v190.json`。本次只生成测试码并上传代码，尚未提交微信审核。

## Web / 微信小游戏 v189 真机背景音乐启动修复 — 2026-09-01

- 根据真机反馈“动作、过关音效正常，但没有背景音乐”单独审计音频链路：确认 512,044 字节的 `assets/bgm-wilds-ambient.wav` 已进入上传包，PCM16/单声道/16 kHz 格式与正常跳跃音一致，音轨 RMS 为 -22.3 dBFS、并非静音；因此排除整体音频权限、资源漏包和 WAV 通用格式不支持。
- 根因收敛到长音轨启动时序：v188 把 `canplay` 当成唯一播放门槛，512 KB 背景音只等待 5 秒；部分真机若漏发或迟发 `canplay`，代码从未调用播放并在超时后销毁实例，而 7.5–28 KB 的动作音可快速就绪，所以只有背景音乐无声。
- v189 只在第一次真实触摸或按键已经提出播放请求后创建背景音乐实例并启用原生 `autoplay`；`onPlay` 作为真正开始播放的权威信号，可在缺少 `canplay` 时直接解除加载看门狗，原有 `canplay → play` 继续保留为第二条兜底路径。冷启动未操作时仍不会自动播放，重复操作仍只复用一个实例。
- 背景音加载看门狗由 5 秒延长到 10 秒；播放启动超时或 Promise 拒绝会释放异常实例，下一次交互或生命周期恢复时重新创建，不再反复重试同一个坏实例。原生循环若意外触发 `ended` 会显式补播。
- 增加微信系统音频中断开始/结束的暂停与恢复；切后台、游戏暂停和销毁仍保持既有单实例生命周期。暂停加载中的实例会取消并在恢复时重建，避免迟到播放越过暂停状态。
- 背景音量由 0.42 提高到 0.62，使偏低频的连续环境音乐在手机外放上更清楚；旧 380ms 点状音序仍永久禁用，跳跃、敲门、开锁和通关动作音继续独立播放。
- 微信冒烟新增“缺少/迟到 `canplay` 仍通过 `autoplay/onPlay` 启动、迟到回调不重复叠加、原生 `ended` 补播、系统音频中断恢复、忽略 autoplay 时仍由 canplay 兜底”等回归；`audio-core` 继续生产默认开启并记录启动策略、超时重建和中断状态，不记录敏感数据。
- 正式主线仍为 20 关，“创造秘境”继续下线且历史本地数据保留；v188 的完整窗口宽地图、安全区 UI 避让及全部关卡功能不变。
- 2026-09-01 已通过微信开发者工具官方 CLI 将同一份代码上传为平台版本 `1.0.189`，上传与预览包均为 811,387 字节；新真机测试二维码保存为 `/Users/bytedance/Documents/ChatGPT/game/wechat-test-qr-v189.jpg`（470×470 JPEG，SHA-256：`94c7eaec784fae9272296e47b6b8ad74c16ca9020f9c1702f7ca4fac27452b48`）。本次只上传代码和生成测试码，尚未提交微信审核。

## 微信小游戏 v188 上传与测试二维码 — 2026-09-01

- 上传前运行确定性同步检查、微信冒烟测试和响应式视口矩阵：20/20 关完整，正式代码版本 v188，“创造秘境”下线，输入、音频、宽屏、安全区和故障恢复回归全部通过。
- 使用微信开发者工具官方 CLI 和正式 AppID `wx6f83b5cdbf65041b`，将代码上传为平台版本 `1.0.188`；上传包大小 810,626 字节，CLI 返回成功。
- 使用同一份 v188 工程生成新的真机测试二维码 `/Users/bytedance/Documents/ChatGPT/game/wechat-test-qr-v188.jpg`；二维码图片为 470×470 JPEG，SHA-256 为 `abc0396adc4cf0d9be4118364948368b472cb2d5d961429a26be32527ee9220e`。
- 上传与预览诊断分别保存为 `wechat-upload-v188.json` 和 `wechat-preview-v188.json`，均记录 810,626 字节；不包含令牌、用户信息或二维码正文。本次尚未提交微信审核。

## 个人网站改动记录全量同步 — 2026-09-01

- 修复个人网站 `shaw.cn` 的《怪奇之原》项目卡片长期硬编码“33 个版本记录”的问题；入口改为“在线阅读全部改动记录”，不再展示会过期的手填数量。
- `brain-egg-changelog.txt` 不再维护独立旧快照，改为与本文件逐字同步，完整保留所有 `##` 顶层记录、`###` 自动子记录和正文；旧文件只有约 10 KB，本轮同步后覆盖当前全部记录。
- 新增可重复运行的 `tools/sync-brain-egg-changelog.js`：默认从游戏项目读取本文件，原子更新公开文本与元数据；`--check` 只检查、不改文件，源缺失或格式错误时保留既有公开记录。
- 同步脚本输出独立 `brain-egg-changelog-sync` JSON 诊断，只记录字节数、SHA-256、标题计数和错误原因，不记录正文或敏感信息；网站 JS 增加缓存版本，避免浏览器继续显示旧卡片。
- 新增专项测试，验证公开文本与本文件逐字一致、元数据计数与哈希匹配、页面不再出现“33 个版本记录”、同步幂等，以及源缺失和只检查模式不会破坏线上文件。

## Web / 微信小游戏 v188 完整窗口宽地图 — 2026-09-01

- 修复带左侧安全区的长屏手机仍出现单侧深色/黑色空条：旧实现把微信 `safeArea` 同时当成地图裁切框，截图中的 `safeArea.left=104` 因而直接变成 104 像素空白。
- 微信逻辑高度继续固定为 1080，但逻辑宽度改为按完整 `windowWidth / windowHeight` 计算；手机越长，冷启动时创建的真实逻辑地图越宽，玩家看到更多世界，而不是把 1920 画面横向拉伸或复制边缘。
- `safeArea` 不再缩短背景、地面或关卡画面，只转换为 `logicalSafeInsets`，用于避让顶部关卡牌、提示/重来/暂停按钮和底部移动/跳跃按钮。
- 以用户截图尺寸 `2403×1080`、左安全区 104 像素建立回归：逻辑宽 2403、`sidePad=241.5`、显示区域从 x=0 覆盖到 x=2403，左侧 gutter 为 0；单屏地面扩到 `[-241.5, 2161.5]`，人物、门、台阶及内部断崖坐标和绝对大小不变。
- 最终画面仍只做一次完整等比绘制，不使用模糊、渐变、边缘像素复制或拖长；16:9 维持原 1920×1080，窄于 16:9 的异常横屏继续等比留边，避免强行变形或无法渲染。
- `viewport` 诊断现在同时记录完整窗口比例、安全区物理/逻辑内边距、布局来源、显示区域和四边 gutter；生产仍为低频日志，可单独启停、筛选和随首页诊断导出，不记录敏感数据。
- 响应式矩阵和微信冷启动冒烟新增非对称安全区真机模型，并继续覆盖 20 关、内部坑、多屏 5760 宽、触控换算、连续背景音乐和故障恢复。正式主线仍为 20 关，“创造秘境”继续下线且历史数据保留。
- 2026-09-01 已使用正式 AppID 编译最终 v188 并生成新的真机预览二维码；预览包 810,626 字节，仅供完整窗口长地图、非对称安全区和音频回归验收，没有上传体验版或提交审核。

## Web / 微信小游戏 v187 连续背景音乐恢复 — 2026-09-01

- 修复 v186 为消除周期点声而把背景音乐整体关闭的问题，恢复持续的游戏背景音乐。
- 新增单一原创连续音轨 `assets/bgm-wilds-ambient.wav`，Web 与微信小游戏使用同一份音源；首尾按同一相位生成并无缝循环，不再把多个短音拼成旋律。
- 第一次真实点击、触摸或键盘操作后才启动背景音乐；重复操作只复用同一实例，不会叠加播放。
- 游戏暂停、页面隐藏或小游戏切后台时暂停并保留播放位置，返回游戏或回到前台后从原位置继续；退出时停止并清理唯一音频实例。
- 微信真机播放严格等待 `canplay`，加载或播放回调超时后释放故障实例并允许下一次操作自动重建；旧实例迟到的 `canplay/error` 事件会按实例身份丢弃，暂停菜单切后台再回来也不会误恢复声音。
- 旧 380ms 短音符序列保持永久禁用，不会重新出现像故障的周期点声。
- 背景音乐使用独立音量；跳跃、落地、敲门、开锁和通关动作音仍走独立通道，其中跳跃继续使用 `assets/sfx-jump-action.wav`。
- `audio` 与微信 `audio-core` 模块增加背景音乐加载、启动、播放、暂停、恢复、拒绝、错误和销毁日志；`__debug.getAudioDiagnostics()` 和首页诊断导出可查看状态、音量、播放位置、资源名和最后错误，且不逐帧或逐音符刷日志。
- 正式主线仍为20关，“创造秘境”继续下线且历史数据保留；v186的精确宽地图、人物动作及第12/15关调整全部保留。
- 2026-09-01 已使用正式 AppID 编译最终 v187 并生成新的真机预览二维码；预览包 809,495 字节，仅供连续背景音乐与动作音并行验收，没有上传体验版或提交审核。

## 网站改动记录同步 — 2026-09-01

- 网页版首页新增“改动记录”入口；只在网页首页显示，进入选关、关卡、Android 或微信小游戏时不会出现。
- 弹窗直接读取项目根目录的 `改动记事本.md`，不再维护第二份手抄内容；每次更新本文件后，网站刷新即可同步。
- 支持按版本、关卡和功能搜索，也可以点击“刷新记录”跳过浏览器缓存重新读取。
- Markdown 内容先转义再渲染，加载失败时提供原始记事本入口，不影响游戏本身启动和操作。
- 新增独立 `website:changelog` 结构化诊断，最多保留 100 条；生产环境详细日志默认关闭，不记录搜索原文或敏感数据。
- 弹窗键盘事件与游戏全局快捷键隔离；每次打开都实时复核页面和平台，加载中的重复刷新复用同一请求，避免串页、串平台和旧响应覆盖。
- 新增网页专项运行测试，固定校验入口范围、单一数据源、加载成功/失败、搜索、刷新、安全转义、Android/微信隔离及诊断留存上限。

## Web / 微信小游戏 v186 动作与精确宽地图 — 2026-08-31

- 停用真机上像故障点声的旧循环音符序列；跳跃改用独立短促发力/衣料动作音，不再复用其他机关声音。
- 裤腿略微延长；走路改为支撑脚贴地、摆动脚抬起，跳跃按上升、顶点、下降分别调整双腿。
- 微信小游戏逻辑宽度改为严格按设备安全区比例计算，不再限制2880；地图按设备需要做长，人物和UI维持原始绝对大小。
- 最终画面只进行一次完整等比绘制，删除边缘像素复制、模糊渐变和拖长填边；安全区外仅显示统一深青灰底。
- 第12关必须从真实地面起手并向左拖动累计720像素才会发现门；天空、人物和门上的拖动不计数，底部左右/跳跃键不会被场景手势抢占，草皮、裂纹和土层斑块会随地面移动，单纯走路时门一直保持在视野外；新增可独立开关的`mechanic:world`日志。
- 第15关墙体加宽至820像素、高112像素，视觉和碰撞统一；正常人物被挡，缩小后可通过。
- 同步网页与微信20关离线包，版本统一为v186；专项网页测试、微信冒烟、响应式矩阵和同步校验通过，创造秘境继续下线且历史数据保留。
- 2026-09-01使用正式AppID重新编译最终v186并生成新真机预览二维码，预览包290,277字节；只用于本轮扫码测试，没有上传体验版或提交微信审核。

## Web / 微信小游戏 v185 自适应宽屏 — 2026-08-31

- 微信小游戏逻辑高度固定为1080，逻辑宽度按安全区宽高比在1920–2880之间计算；19.5:9、20:9等设备增加真实可见地图宽度。
- Canvas保持统一比例呈现，人物、门、平台、箱子和操作按钮不横向拉伸；网页与Android继续使用固定1920×1080。
- 有限单屏关只延展最左、最右地面外边，内部坑、出生点、门和机关坐标保持；多屏地图继续按每屏1920计算。
- 极端超宽使用深青灰底与非交互柔和边缘，窄横屏继续等比contain。
- 新增独立`viewport`日志、`getViewportLayout`、`inspectLevelViewport`及响应式矩阵测试，覆盖16:9、19.5:9、20:9、安全区偏移、DPR3、窄屏与超宽。
- 小程序正式版继续下线“创造秘境”，历史本地数据保留；版本日期为2026-08-31。
- 柔边和安全区外的新触摸不会进入游戏；从画面内起手的手指移出后仍会收到松手，避免方向或按住机制卡住，并有独立 `viewport/touch-start-outside-display` 诊断。
- 使用正式 AppID 刷新微信开发者工具并生成包含触控隔离修复的新 v185 真机预览二维码，预览包279,115字节；未上传体验版、未提交审核。

## 全关卡坑口改为垂直深坑 — 2026-08-28

- 根据截图红线标注，主线与第 13–42 关试玩的坑口左右壁改为垂直黑色轮廓。
- 深色坑体一直延伸到画面底部，试玩控制区中央不再把坑截成浅槽。
- 不修改坑宽、平台坐标、死亡判定或按钮热区；`render/drop-terrain-layout` 样式更新为 `vertical-shaft-v1`。
- 网页主线升级为 v173，连续评审缓存升级为 v6，并补充垂直边界与功能守恒测试。

## 第13–42关问号提示具体化 — 2026-08-28

- 画面直接显示的 30 个关卡标题保持不变。
- 问号提示改为可执行说明：写明操作对象、动作方式与成功条件。
- 密码和音高等需要玩家观察的最终答案不直接泄露。
- 更新连续评审缓存与运行版本，并补充提示长度、关卡 15 操作描述测试。

## v1.0 — 2026-08-12
> 初始化：v1.0 版本号起点，后续自动记录从 v1.1 开始递增


### v1.1 自动记录 — 2026-08-12 01:00:30
- 工具: `Edit`
- 项目: **game**
- 文件: `~/Documents/trae_projects/game/web_preview/game.js`
- 改动: 修改「game.js」中的：drawComplete

### v1.2 自动记录 — 2026-08-12 01:00:32
- 工具: `Write`
- 项目: **game**
- 文件: `~/Documents/trae_projects/game/改动记事本_dummy.txt`
- 改动: 新建/全量写入文件「改动记事本_dummy.txt」

### v1.3 自动记录 — 2026-08-12 01:00:34
- 工具: `DeleteFile`
- 项目: **game**
- 文件: `~/Documents/trae_projects/game/_tmp.tmp`
- 改动: 删除文件：~/Documents/trae_projects/game/_tmp.tmp

### v1.4 自动记录 — 2026-08-12 01:00:37
- 工具: `Edit`
- 项目: **game**
- 文件: `~/Documents/trae_projects/game/版本记录.txt`
- 改动: 修改文件「版本记录.txt」

## v21.0 — 2026-08-13
- 调整右上角中间“重来”按钮：回转箭头的圆弧更大、更粗、更接近整圈，箭头改为单个饱满大箭头。
- `GAME_CODE_VERSION` 与 `index.html` 缓存参数同步升级到 v111。

## v22.0 — 2026-08-13
- 按参考图重做游戏 HUD：关卡吊牌与三个功能按钮改为顶部左侧横排，标题居中偏右。
- 底部移动/跳跃键改为黑色手绘方块与白色粗线箭头，左右键拉开、跳跃键靠右。
- `GAME_CODE_VERSION` 与 `index.html` 缓存参数同步升级到 v112。

## v23.0 — 2026-08-13
- 角色改为参考图的竖向豆蛋比例、黑色椭圆眼与露齿笑脸。
- 第 1 关重排为中部出生平台、右上门平台、底部白色控制区地面，并同步 JSON/fallback 碰撞数据。
- 平台增加厚黑草皮顶边，门放大并增加粗线双层手绘轮廓。
- `GAME_CODE_VERSION` 与 `index.html` 缓存参数同步升级到 v113。

## v24.0 — 2026-08-13
- 修复关卡 JSON 仍命中旧缓存导致新版布局不生效：请求统一追加代码版本参数并使用 `no-store`。
- 补齐兼容加载路径的第 1 关新布局 fallback。
- `GAME_CODE_VERSION` 与 `index.html` 缓存参数同步升级到 v114。

## v25.0 — 2026-08-13
- 按局部截图精修角色：上窄下圆的矮胖豆蛋轮廓、弯曲外展手臂、椭圆眼和露齿笑脸。
- 门身改为上窄下宽的手绘梯形与双层错位描线，把手移到左侧中部。
- `GAME_CODE_VERSION` 与 `index.html` 缓存参数同步升级到 v115。

## v26.0 — 2026-08-13
- 双腿明显加长，站立和空中状态都能清楚看到。
- 跳跃时手臂向上外展，并按真实时间左右交替挥动；手臂根部保持连接身体。
- `GAME_CODE_VERSION` 与 `index.html` 缓存参数同步升级到 v116。

## v27.0 — 2026-08-13
- 修复顶部问号、重来、暂停三个按钮只有图形、没有点击逻辑的问题。
- 问号切换提示面板，重来复位当前关，暂停冻结并继续游戏。
- `GAME_CODE_VERSION` 与 `index.html` 缓存参数同步升级到 v117。

## v28.0 — 2026-08-13
- 第 1 关出生点移到左侧地面，并同步 JSON 与两套 fallback。
- 暂停面板新增“回到选关”和“返回游戏”两个可点击手绘按钮。
- `GAME_CODE_VERSION` 与 `index.html` 缓存参数同步升级到 v118。

## v29.0 — 2026-08-13
- 修复平台高度超过跳跃能力：地面到一级平台改为 116px，一级到门平台改为 130px。
- 同步下移门，并更新关卡 JSON 与两套 fallback。
- `GAME_CODE_VERSION` 与 `index.html` 缓存参数同步升级到 v119。

## v30.0 — 2026-08-13
- 回滚 v119 的台阶和门位置，恢复 v118 布局。
- 跳跃初速度由 `-780` 提高到 `-1080`，理论跳跃高度由约 152px 提高到约 291.6px。
- `GAME_CODE_VERSION` 与 `index.html` 缓存参数同步升级到 v120。

## v31.0 — 2026-08-13
- 双腿继续加长约 6px，并同步校正人物视觉位置，脚底保持踩在平台/地面上。
- 跳跃时左右腿和脚端向两侧外展，形成明显张腿动作。
- `GAME_CODE_VERSION` 与 `index.html` 缓存参数同步升级到 v121。

## v32.0 — 2026-08-13
- 待机加入轻呼吸和周期眨眼，走路加入弹动和弧线嘴，跳跃保持兴奋笑脸、举手张腿。
- 台阶顶部黑色草皮加厚，小草整体放大并加粗。
- `GAME_CODE_VERSION` 与 `index.html` 缓存参数同步升级到 v122。

## v33.0 — 2026-08-13
- 水平移动速度从 420 降至 300，满速步频降至约 1.45Hz。
- 走路每个周期身体向上弹起一次，形成慢速“一步一蹦”效果。
- `GAME_CODE_VERSION` 与 `index.html` 缓存参数同步升级到 v123。

## v34.0 — 2026-08-16
- 小草改为白色内腔、黑色闭合轮廓，并同步 Web 与 Godot 绘制。
- 阶梯改为参考图中的白色长胶囊、厚黑草皮、空心双叶芽与浅横纹。
- `GAME_CODE_VERSION` 与 `index.html` 缓存参数同步升级到 v124。

## v35.0 — 2026-08-16
- 皮蛋统一为圆润蛋形、弯眉毛、黑色椭圆眼、腹部弧线及参考动作姿势。
- 优化露齿笑嘴型：嘴角改为连续 C 形圆弧，牙齿分隔改为两条近似竖直的轻弧线。
- 眉毛加入待机、走路、起跳和下落动画，人物线条整体略微加粗。
- `GAME_CODE_VERSION` 与 `index.html` 缓存参数同步升级到 v125。

## v36.0 — 2026-08-16
- 新增跳跃、落地、开锁和通关程序化音效。
- 新增低音量循环背景音乐，首次交互后启用，页面切到后台时暂停发声。
- `GAME_CODE_VERSION` 与 `index.html` 缓存参数同步升级到 v126。

## v37.0 — 2026-08-16
- 新增第 2 关“想办法跳上去”：左门出生、可推动木箱、右上出口。
- 木箱支持重力、平台碰撞、玩家推动、站立和重开复位。
- 第二关增加独立提示及封闭边界规则，人物不会坠落死亡。
- `GAME_CODE_VERSION` 与 `index.html` 缓存参数同步升级到 v127。

## v38.0 — 2026-08-16
- 新增第 3 关“门被锁住了!”：地面钥匙、右侧锁链门、左上门出生。
- 碰到钥匙后钥匙沿弧线旋转飞向门，到达后自动解锁；未解锁碰门无提示且不通关。
- 第三关启用防坠落规则并增加独立提示。
- `GAME_CODE_VERSION` 与 `index.html` 缓存参数同步升级到 v128。

## v39.0 — 2026-08-16
- 新增第 4 关“一次不够，再来一次!”：左右平台与中间深坑。
- 仅第四关支持二段跳，落地后恢复两次跳跃机会；问号提示明确可连续跳两次。
- `GAME_CODE_VERSION` 与 `index.html` 缓存参数同步升级到 v129。

## v40.0 — 2026-08-16
- 分段地面的悬崖侧壁增加粗黑色手绘竖轮廓。
- 所有关卡背景统一为 48px 浅网格，每四格增加一条主网格线。
- 第四关标题下移一行并严格水平居中。
- `GAME_CODE_VERSION` 与 `index.html` 缓存参数同步升级到 v130。

## v41.0 — 2026-08-16
- 新增 Android 原生 WebView 包装工程，离线内置当前网页游戏及四关关卡数据。
- 手机端启用横屏、沉浸式全屏、触摸控制和本地音频播放，并生成已签名可安装 APK。
- `GAME_CODE_VERSION` 与 `index.html` 缓存参数同步升级到 v131。

## v42.0 — 2026-08-16
- 新增第 5 关“要有礼貌哦～”：人物从左门出生，右侧目标门不会因人物接触而直接通关。
- 点击右门均显示“咚”字扩散特效并播放低沉敲门声；只有人物站在门前时才累计有效敲门，第二次有效敲门后通关。
- 第五关提示采用含蓄文案，不直接透露需要敲门两次；同步更新 Android 离线安装包。
- `GAME_CODE_VERSION` 与 `index.html` 缓存参数同步升级到 v132。

## v43.0 — 2026-08-16
- 首页主按钮文案由“选择关卡”改为“开始游戏”，点击后仍进入选关页面。
- 第五关提示改为“走到门前，试着敲敲门。”，直接点明敲门操作但不泄露敲门次数。
- 同步更新 Android 离线安装包，`GAME_CODE_VERSION` 与 `index.html` 缓存参数升级到 v133。

## v44.0 — 2026-08-16
- Android 应用新增手绘皮蛋启动图标，安装后在手机桌面、应用列表和系统应用信息中显示。
- Android 构建流程增加矢量资源编译，重新生成并签名 v134 APK。
- `GAME_CODE_VERSION` 与 `index.html` 缓存参数同步升级到 v134。

## v45.0 — 2026-08-16
- 修复 Android 版关卡全部显示“待开发”：新增脚本内置的 1–5 关数据，绕开 WebView 对 `file://` JSON 请求的限制。
- Android 版启用独立全屏样式，画布铺满屏幕并隐藏网页预览提示条、圆角和阴影。
- 启动图标重绘为更醒目的黑底白色皮蛋头像；重新生成并签名 v135 APK。
- `GAME_CODE_VERSION`、样式和脚本缓存参数同步升级到 v135。

## v46.0 — 2026-08-16
- 在电脑上安装可长期使用的 JDK 17、Android SDK、Android 35 ARM64 模拟器和 Google APIs 系统镜像。
- 创建横屏 GPU 加速模拟设备 `Smart_Egg_Phone`，安装并实测 v135：首页、选关状态与第一关场景均正常。
- 新增一键启动模拟器及安装最新 APK 的脚本和使用说明。

## v46.1 — 2026-08-16
- Android 图标改为标准自适应图标，移除系统为旧式图标自动添加的蓝色外圈。
- 图标严格只使用黑白两色，皮蛋轮廓更圆、眼睛和笑脸更大，整体更 Q。
- 重新生成并签名 v136 APK，`GAME_CODE_VERSION` 与页面资源缓存参数同步升级到 v136。

## v46.2 — 2026-08-16
- 按用户参考图重新设计手机启动图标，保留细手绘皮蛋造型、双眉毛、椭圆眼睛、两颗牙和短手脚。
- 去除参考图中的方格、地面和小草，采用白色背景、黑色手绘轮廓，严格使用纯黑白配色且不含蓝色圆环。
- 重新生成并签名 v137 APK，`GAME_CODE_VERSION` 与页面资源缓存参数同步升级到 v137。

## v46.3 — 2026-08-16
- 手机启动图标不再使用重绘稿，改为直接使用用户上传的原始截图，完整保留截图中的方格、地面、小草和皮蛋造型。
- 重新生成并签名 v138 APK，`GAME_CODE_VERSION` 与页面资源缓存参数同步升级到 v138。

## v46.4 — 2026-08-16
- 新增详细诊断日志：记录会话与设备信息、每次键盘/触摸动作、游戏状态、人物物理快照、关卡资源请求、存档、音频、异常和离线后端状态。
- 首页右上角新增“导出日志”；网页下载 JSON，Android 保存到系统“下载/Smart Egg”目录，便于后续提交日志排查。
- 重写手机多点触控，移动键和跳跃键可同时按住，松开其中一根手指不会中断另一个动作。
- 第五关每次点击门均播放更明显的“咚”音效并写入有效/无效敲门日志。
- Android 构建脚本改为将日志桥接所需的全部 Java 内部类打入 DEX，避免新增原生日志导出后启动崩溃。
- 重新生成并签名 v139 APK，`GAME_CODE_VERSION` 与页面资源缓存参数同步升级到 v139。

## v46.5 — 2026-08-16
- 新增第六关“密码是？”：四个问号方块依次藏有 3、1、2、5 朵花，每次顶出一朵，清空后变“×”；走到右门弹出数字面板，输入 3125 通关。
- 首页“开始游戏”改为“主线关卡”，并新增“创造模式”入口。
- 完成第一期本地关卡生态：自定义关卡列表、关卡生成器、平台/出生门/终点门/木箱/钥匙摆放、本地保存、即时试玩、JSON 导入导出。
- 创作、摆放、保存、导入导出和试玩过程全部接入详细诊断日志。
- 重新生成并签名 v140 APK，`GAME_CODE_VERSION` 与页面资源缓存参数同步升级到 v140。

## v46.6 — 2026-08-16
- 第六关顶出的花朵改为短暂飘出后消失，不再常驻显示，玩家需要自行记忆每个方块的数量。
- 密码面板右上角新增独立关闭按钮；关闭后留出移动时间，避免仍站在门口时立即重复弹窗。
- 创造模式保存后自动返回自定义关卡列表；每张关卡卡片提供“玩”和“编辑”两个入口。
- 自定义关卡新增特殊玩法选择：普通、多跳、敲门、顶花密码、钥匙，复用主线关卡的对应机制。
- 重新生成并签名 v141 APK，`GAME_CODE_VERSION` 与页面资源缓存参数同步升级到 v141。

## v46.7 — 2026-08-16
- 第六关密码不再使用独立写死值，改为实时读取四个问号方块的花朵数量并拼成密码，保证谜面与答案始终一致。
- 创造模式编辑器新增“撤销”按钮，可撤销最近一次组件摆放或特殊玩法切换，最多保留 30 步。
- 密码正确与错误日志增加实际推导答案字段，方便后续定位计数问题。
- 重新生成并签名 v142 APK，`GAME_CODE_VERSION` 与页面资源缓存参数同步升级到 v142。

## v46.8 — 2026-08-17
- 创造模式特殊玩法改为多选，可同时组合多跳、敲门、顶花密码和钥匙；“普通”用于一键清空玩法组合。
- 自定义关卡列表会展示组合玩法名称，例如“多跳＋钥匙”，试玩时各玩法同时生效。
- 删除关卡编辑器的“导出”按钮，并删除创造模式列表的“导入关卡”入口。
- 保存关卡后自动返回创造模式关卡列表，新关卡立即提供“玩”和“编辑”入口。
- 重新生成并签名 v143 APK，`GAME_CODE_VERSION` 与页面资源缓存参数同步升级到 v143。

## v46.9 — 2026-08-18
- 创造模式保存改为一键直接保存，移除依赖系统 `prompt` 的命名弹窗，修复部分 Android WebView 点击保存没有反应的问题。
- 新建关卡自动按顺序命名为“我的脑洞关卡 N”，重复保存会更新原关卡，不会重复创建。
- 保存成功后自动返回创造模式列表，显示“已保存，点击‘玩’开始体验”，并立即提供“玩/编辑”入口。
- 重新生成并签名 v144 APK，`GAME_CODE_VERSION` 与页面资源缓存参数同步升级到 v144。

## v47.0 — 2026-08-18
- 创造模式每张自定义关卡卡片右上角新增“×”删除按钮。
- 点击删除后显示游戏内二次确认窗口，明确展示关卡名称；选择“取消”不会修改数据，选择“确认删除”才从本地列表永久移除。
- 删除请求、取消和确认结果全部写入详细诊断日志。
- 重新生成并签名 v145 APK，`GAME_CODE_VERSION` 与页面资源缓存参数同步升级到 v145。

## v47.1 — 2026-08-18
- 修复自定义关卡暂停后点击返回错误进入编辑器的问题，现在统一返回“创造模式关卡列表页”。
- 自定义试玩的暂停按钮文案改为“回到创造模式”，主线关卡仍显示“回到选关”。
- 建立全局页面名称映射，诊断日志增加当前页面名称，并新增 `页面命名.md` 作为后续沟通标准。
- 重新生成并签名 v146 APK，`GAME_CODE_VERSION` 与页面资源缓存参数同步升级到 v146。
## v147（2026-08-19）

- 优化安卓模拟器与手机端 Canvas 性能：内部渲染缓冲降至 0.70 倍，逻辑尺寸与全屏比例保持不变，像素填充量约减少 51%。
- Canvas 使用不透明上下文，减少 WebView 透明合成开销。
- 实测并排除了离屏 Canvas 缓存与强制静态页降帧方案：安卓模拟器的 Skia OpenGL 管线在这两种策略下反而更慢，最终采用兼容性更好的直接绘制。
- 修复 `android_test_env/start-emulator.sh` 中中文标点紧邻变量导致的启动失败；重启模拟器可清除异常 GPU 状态。
- 冷启动实测：中位帧耗时约从 65ms 降至 30ms，现代卡顿帧占比约从 99.8% 降至 10.4%。
- 发布 v147 APK，版本名 47.2。
## v148（2026-08-19）

- 新增第七关“这个门有点不对劲”：人物靠近后目标门以同速远离，人物跳跃时门同步跳跃。
- 必须持续按住目标门将其固定，再同时控制人物走到门前才能通关；松手后门恢复逃跑。
- 支持手机多指操作以及电脑鼠标按住，详细日志记录元素按住和松开状态。
- 创造模式新增“会动”特殊玩法，可与现有玩法多选组合；选择后灰屏指定任意已布置元素。
- 编辑状态以 `≋` 标记已绑定元素，实际游玩时不显示提示；支持多个元素分别被多指同时固定。
- 发布 v148 APK，版本名 48.0。
## v149（2026-08-20）

- 所有门增加两只手绘小脚；第七关逃跑时双脚交替迈步，按住后立即站稳。
- 第七关改为超长横向世界（逻辑宽度 1,000,000），取消横向屏幕边界并加入平滑镜头跟随，可持续追逐很长距离。
- 背景音乐主旋律和低音音量整体提高约 80%，操作音效保持原音量。
- 创造模式加入离线关卡码：关卡卡片新增“分享”，支持一键复制 `SE1` 关卡码。
- “生成自定义关卡”上方新增“输入关卡码”，仅支持复制粘贴；导入前校验格式、完整性、必需元素、元素总数和终点门。
- 导入成功后可立即玩或保存到我的关卡；允许继续编辑、二次创作并生成新的分享码。
- 发布 v149 APK，版本名 49.0。

## v150（2026-08-20）

- 第七关的门只在实际逃跑时显示小脚，停止、被按住以及普通门均不显示；跑动时门身平滑上提，脚底不再陷入地面。
- 逃跑门改为完整连续跟随人物的跳跃轨迹，移除跳跃顶部限幅，消除门在最高点停顿、卡住的观感。
- 无限横向关卡镜头以人物出生点为原点，可持续向右奔跑，同时支持人物向左时门同步向左。
- 第七关地面小草固定在世界坐标，奔跑时随镜头滚动，形成清晰的速度与距离参照。
- 发布 v150 APK，版本名 50.0。

## v151（2026-08-20）

- 修复第七关人物转向左跑时门没有同步左移：逃跑门现在直接读取当前帧左右输入，不再依赖下一阶段才更新的 `moveDir`。
- 目标门首次进入接近范围后永久激活逃跑状态，转身导致距离暂时超过触发线时也不会失去方向响应。
- 发布 v151 APK，版本名 50.1。

## v152（2026-08-20）

- 修复第七关点击“重来”后目标门停留在跑走位置的问题。
- 每次进入或重开关卡均从原始缓存深拷贝全新运行数据，门恢复初始坐标，同时清除逃跑激活、跑动抬升和脚步等临时状态。
- 同一机制也保证木箱、钥匙及其他运行中会改变的元素能够可靠复位。
- 发布 v152 APK，版本名 50.2。

## v153（2026-08-20）

- “输入关卡码”改为真实文本输入弹窗，点击后立即聚焦输入区并拉起手机系统键盘；支持粘贴、错误提示、取消和读取关卡。
- 关卡编辑器新增“左右无限”和“向上扩展”两项地图范围能力，可单独或同时启用。
- 编辑器新增左移、右移、上移、下移和回原点视野按钮；移动视野后可继续在扩展区域摆放平台、门、木箱和钥匙。
- 自定义关卡试玩支持横向持续镜头和向上纵向镜头，出生点在扩展区域时也会自动进入可见画面。
- 左右无限地图自动生成覆盖左右约 200 万逻辑像素的地面，支持类似第七关的持续长跑。
- 扩展范围设置会随保存和关卡码一起保留，旧自定义关卡仍按普通单屏模式兼容运行。
- 发布 v153 APK，版本名 50.3。

## v154（2026-08-21）

- 关卡编辑器新增“调整地图”按钮，点击后进入全图缩略模式，自动把当前完整地图缩放到一屏展示。
- 全图模式新增“＋左、－左、＋右、－右、＋上、－上”六个范围按钮，每次按一整屏扩展或收缩，支持连续多次点击。
- 地图外框、分屏线、平台、门、木箱和出生点会在缩略图中实时更新，可直观看到地图变大或变小。
- 减少边界时会检查该区域是否仍有元素；存在元素则阻止收缩并提示，避免误删创作内容。
- 普通编辑视野和试玩镜头改为读取实际地图边界，支持在新增的左、右、上区域继续摆放和游玩。
- 地面宽度、世界尺寸、玩家/动态元素边界会随地图范围同步变化；范围数据随保存和关卡码保留。
- 发布 v154 APK，版本名 50.4。
## v155（2026-08-21）

- 创造模式关卡编辑器工具栏新增“选择”功能。
- 在选择模式中先点击已有平台、出生门、终点门、木箱、钥匙或问号块，再点击地图目标位置即可移动。
- 被选中的元素显示黄色虚线框；移动动作进入撤销历史并写入详细日志。
- 移动出生门会同步更新玩家出生坐标，地面不参与选择，避免破坏地图底板。
- 新增独立目录 `fantasy_level5_demo_2`，作为第二版原创奇幻赛璐璐第五关视觉试验，加入与主游戏相对尺寸一致的人物、阶梯和门，不参与主游戏及 APK 构建。
- 发布 v155 APK，版本名 50.5。

## v156（2026-08-25）

- 正式选定 F4 为主游戏 UI：背景更换为清透高空、暖色天际、远方台地、薄雾谷地与河光的开放旷野。
- 活动地面改为高处悬崖，加入草顶、金色岩沿、岩壁渐变、裂纹和岩层高光。
- 主角、遗迹门、阶梯、木箱和全局 UI 统一为暖色奇幻色彩、深青轮廓与金色点缀。
- 七个主线关卡启用原创地点式标题，背景音乐更换为原创奇幻五声音阶循环。
- 保留主线 1–7 关、创造模式、关卡码、日志、原主线存档和全部现有玩法，不导入独立 UI 试验版的本地存档。
- `GAME_CODE_VERSION` 与页面资源缓存参数同步升级到 v156；主页游戏名暂保持“大聪明脑洞蛋”，等待正式命名。
- 同步更新 Android 离线资源和启动背景，构建并签名 v156 APK，Android `versionName` 为 50.6。

## v157（2026-08-26）

- 修复通关自动进入下一关、重来或死亡重试时，上一关仍按住的移动/跳跃输入被带入新关，导致人物自动走动或误跳的问题。
- 新关开始时统一清空键盘、鼠标与多点触控控制状态，并重置人物 `moveDir`、`wantJump`。
- 对切关瞬间仍未松开的实体键增加“等待松开”保护：浏览器后续的 repeat `keydown` 不再重新激活移动，收到对应 `keyup` 后恢复正常输入。
- 日志新增 `input/level-input-reset` 与 `input/carried-keydown-blocked`，便于排查后续跨关输入问题。
- `GAME_CODE_VERSION` 与页面资源缓存参数同步升级到 v157；Android `versionCode` 升级为 157、`versionName` 升级为 50.7，并完成 v157 APK 构建与签名校验。

## v158（2026-08-26）

- 修复屏幕方向键在窗口失焦、切换应用或漏收 `mouseup` 时持续保持按下，导致人物一路顶到左边界并无法正常反向移动的问题。
- 键盘、鼠标和每个触点改为独立输入来源，再聚合为左右与跳跃状态；释放一个来源不再误清仍按住的其他键或手指。
- 新增 `window.blur`、页面隐藏、`pagehide` 和全局 `mouseup` 清理兜底；切关与暂停仍保留“等待实体键松开”保护，失焦则丢弃陈旧 held/blocked 状态，避免下一次按键被错误屏蔽。
- 鼠标方向按钮新增 `mouse-control-down/up` 详细日志，所有输入重置记录原因、重置前后状态、活动来源、实体键和触点，导出日志可直接还原卡键现场。
- 输入关卡码时跳过游戏方向键映射；新增 `?inputtest=1` 输入回归模式，覆盖键盘/鼠标/触屏失焦、多键同方向、键鼠混合、多触点、移动加跳跃、文本输入、跨关拦截和相反方向共 10 项测试。
- `GAME_CODE_VERSION` 与页面资源缓存参数同步升级到 v158；Android `versionCode` 升级为 158、`versionName` 升级为 50.8，并重新构建 v158 APK。

## v159（2026-08-26）

- 七个主线关卡的关内标题去掉间隔点和后半句，仅保留短标题，并全部改为水平居中。
- 第四关标题由含义较弱的“断崖回声”调整为更能提示双跳玩法的“断崖双跃”。
- 第四关问号提示补充具体操作：“跳起后，在空中再按一次跳跃，就能越过断崖。”
- `GAME_CODE_VERSION` 与页面资源缓存参数同步升级到 v159；Android `versionCode` 升级为 159、`versionName` 升级为 50.9。

## 品牌资源（2026-08-26）

- 正式选定 F4 风格小程序头像，保存为 `branding/mini-program-avatar.png`。
- 头像采用深青遗迹门、金色符文、红色披风与单层浅蓝纯色背景；不再使用白色边角、黄色底层或渐变分层。
- 将头像门上的装饰罗盘替换为游戏地图门所用的金色圆环十字符号，确保品牌图标与关卡内元素一致。
- 新增 `branding/README.md` 固定使用约定：后续小程序默认使用该头像，其余方案仅作为候选稿保留。
- 本次仅归档品牌资源，不修改网页游戏逻辑、代码版本或 APK 版本。

## 微信小游戏备案截图（2026-08-26）

- 新建 `微信小游戏备案/截图`，归档主界面、主线选关、主线第 1–7 关、密码面板、创造模式列表和地图编辑器共 12 张截图。
- 截图统一为 1280×720 PNG，单张大小均在微信后台要求的 60KB–5MB 范围内。
- 每张截图均显示当前申报名称“怪奇之原”，并按内容编号和中文命名，方便直接选取上传。
- 新增 `微信小游戏备案/截图清单.md`，列明每张截图内容以及场景、玩法、系统、背景、角色、情节和特点的建议上传组合。
- 截图由独立临时备案预览生成，不修改正式主游戏、Android 离线资源、代码版本或 APK 版本。
- 按“不允许重复截图”的备案要求，再补充 10 张独立状态图：空中跳跃、推箱过程、钥匙飞行、二次跳跃、敲门“咚”特效、顶出花朵、逃跑门奔跑、提示窗口、暂停窗口和地图范围全览。
- 当前资料包共 22 张；所有图片哈希均不同，并重新确认均为 1280×720 PNG、单张 60KB–5MB。

## 微信小游戏 v160（2026-08-26）

- 新建完全隔离的 `wechat_minigame` 工程，现有网页 v159、Android 离线资源和 APK 均不改动。
- 复用七个主线关卡、创造模式、离线关卡码、详细日志、F4 UI、原创音乐和全部玩法机制。
- 新增微信小游戏平台适配层：第一张 Canvas 负责上屏，第二张离屏 Canvas 保持 1920×1080 逻辑坐标；根据横屏安全区等比例呈现，避免刘海遮挡按钮。
- 接入 `wx.onTouchStart/End/Cancel` 多点触控，保留移动与跳跃同时按下以及第七关按住逃跑门的操作。
- 接入微信本地存储、系统键盘、剪贴板、前后台生命周期、网络状态、错误与未处理 Promise 事件。
- 优先使用微信 WebAudio；不支持时以三个内置 WAV 音源配合 `InnerAudioContext` 兼容背景音乐和跳跃、落地、开锁、敲门、通关音效。
- 日志导出改为写入微信用户目录，环境支持时分享文件，否则复制日志文本到剪贴板。
- 七关数据使用包内 `levels.js`，不再依赖 HTML、CSS、本地 JSON fetch 或业务服务器；第 8–10 关继续显示为待开发。
- 新增 Node.js 启动冒烟测试和原有 10 项输入回归测试；工程包约 380KB，低于小游戏主包限制。

## 网页主线 v164：第 8–12 关正式关卡（2026-08-27）

- 将脑洞机制试玩升级为网页主线正式第 8–12 关，补齐 5 份关卡 JSON、离线关卡包和 12 关选关布局。
- 第 8 关：拖动光源改变主角影子，让影尖触发远处机关后开门。
- 第 9 关：横向地图扩展为约一百万逻辑单位，人物可持续向右奔跑；移动时门保持距离，停止操作后门才逐渐靠近。
- 第 10 关：可拖动标题中的“路”字落入断口，形成能够站立的桥面。
- 第 11 关：重来会逐次保留并增加箱子，凑成三级台阶；提示改为明确说明“多重来几次”，并支持长文案自动换行。
- 修复第 11 关箱子偶尔能站、偶尔穿过的问题：静态箱子不再受重力或推力影响，落脚面改为按上一帧位置收集候选面并稳定选择最高合法表面，避免受数组顺序和帧率影响。
- 第 12 关：拖动画面会真实移动世界与终点门；删除“远处安静得看不见尽头”这一排明示文字。
- 扩充分模块诊断日志：`input`、`physics`、`collide`、`mechanic`、`storage`、`runtime` 可分别启停和过滤；逐帧物理日志生产默认关闭，可用 `?verbose=1` 单独开启；日志维持会话、时间、帧号和非敏感上下文。
- 独立试玩目录同步到 v2，便于不受主线解锁进度影响地快速体验五个机制。
- 新增第 8–12 关数据、离线包、碰撞和日志开关冒烟检查；JavaScript 语法、两组冒烟测试及补丁空白检查通过。
- 本次只更新网页主线和独立机制试玩；Android APK 与微信小游戏工程尚未同步。

## 第 8 关视觉调试修复（网页 v165 / 试玩 v3，2026-08-27）

- 根据实际试玩截图复现：顶部提示与重来按钮只有底框、没有图标；第 8 关同时出现装饰太阳和机关太阳；影子缩放后与地面线重合，几乎不可见。
- 根因一：绘制按钮背景会覆盖 Canvas 的 `fillStyle`，图标随后使用了与按钮相同的浅黄色；现已在每个图标绘制前显式恢复深色。
- 根因二：通用背景太阳与第 8 关可拖太阳重复；试玩第 8 关现只绘制机关太阳。
- 根因三：影子梯形仅约 12 像素高；试玩与正式主线均加宽并抬高影尖，同时增加轻描边，确保小窗口仍能辨认方向。
- 诊断日志未发现脚本异常，问题限定为绘制状态与视觉层级；新增对应源码回归断言，并完成浏览器画面复查。
- 本次只更新网页主线与独立试玩；Android APK、微信小游戏工程尚未同步。

## 试玩页顶部按钮与关卡列修复（试玩 v4，2026-08-28）

- 提示和重来按钮不再使用 `?`、`↻` 字体字符，改为 Canvas 贝塞尔线、圆弧和箭头三角形直接绘制，规避部分内置浏览器缺字或颜色状态异常。
- 删除右侧第 8–12 关竖排按钮的绘制与点击热区，画面恢复干净；玩家通关后仍可按顺序进入下一关。
- 保留 `__brainstormDemo.enterLevelById(id)` 作为仅供开发调试的按编号跳关入口。
- 新增矢量图标与右侧入口删除的源码回归检查；本次不修改正式主线、Android APK 或微信小游戏工程。

## 试玩页重来图标修正（试玩 v5，2026-08-28）

- 实际截图显示 v4 重来图标仍像缺口圆环，箭头方向不清楚。
- 改为约 288° 的回转圆弧，并在左上角使用固定几何的大型实心箭头；扩大缺口，避免缩小时再次看成字母 C 或普通圆环。
- 增加箭头结构回归检查；本次不修改正式主线、Android APK 或微信小游戏工程。

## 第 8 关必须拖动太阳修复（网页 v166 / 试玩 v6，2026-08-28）

- 复现无需拖动太阳也能开门：太阳固定时，人物走动仍会改变影尖横坐标，影尖经过机关后原逻辑会直接累计 0.65 秒并解锁。
- 太阳初始横坐标从 880 移到 100；试玩第 8 关出生横坐标与正式关统一为 420，初始影尖明确落在机关右侧。
- 新增太阳拖动资格状态：指针拖动距离达到 12 个逻辑像素才置为有效；机关计时同时要求“已真实拖动太阳”和“影尖命中机关”。
- 新增 `mechanic/sun-drag-armed`（试玩）与 `mechanic/shadow-drag-armed`（正式主线）诊断事件；复位时资格状态一并清空。
- 网页主线与独立试玩增加回归检查，确认初始影子不命中、未拖动时判定被门槛拦截；Android APK 与微信小游戏工程本次暂未同步。

## 试玩页恢复主线返回路径（网页 v167 / 试玩 v7，2026-08-28）

- 用户发现新增关卡试玩页没有“回到菜单”，并误以为 UI 改版删掉了主线功能。核对 v159 与当前正式主线后确认：首页、选关、暂停、创造模式、编辑器和日志仍在；缺失只发生在独立 `brainstorm_levels_demo`。
- 试玩页恢复顶部暂停按钮和暂停遮罩，提供“返回游戏”“回到主线选关”；支持 `P` / `Esc`，暂停时只清理实时输入，不重置关卡状态。
- 正式主线新增 `?screen=levelSelect` 启动路由，供试玩页直接返回完整选关页；普通启动路径仍保持首页不变。
- 新增 `mainline-function-parity-test.js` 功能守恒测试，后续 UI 改版或新增关卡必须保留首页、选关、暂停返回、创造模式、编辑器、控制和日志功能。
- 功能对照还发现第 12 关全景拖拽会先于旧按钮处理，吞掉顶部提示、重来、暂停以及弹层点击；新增统一 UI 手势屏障和 `scene-gesture-bypassed-ui` 日志，保证新玩法只接管空白场景区域。
- 本次不修改现有 12 关玩法数据、Android APK 或微信小游戏工程。

## 提示状态与第 9 关可达性修复（网页 v168 / 试玩 v8，2026-08-28）

- 全部 12 关的问号提示改为具体操作步骤，明确移动、跳跃、敲门、密码、按住逃跑门、拖太阳、静止等待、拖“路”字、重来留箱和拖动世界的执行方法。
- `helpVisible` 原为跨关共享状态，而统一关卡加载入口未复位；现每次进入、重来或切换下一关都默认关闭提示，并记录 `state/help-default-closed`。
- 第 9 关无法通关的根因是门最终停在人物右侧 105 像素，碰撞重叠面积只有约 1200，低于通关阈值 4800；改为可配置的 `approach_offset=55`，静止后能够可靠进入通关区域。
- 第 9 关新增 `mechanic/stillness-door-overlap-ready` 日志，记录实际重叠面积、阈值和双方坐标，区分“视觉贴近”与“物理可达”。
- 冒烟测试新增 10 秒静止模拟，确认第 9 关最终重叠达到阈值；离线关卡包一致性检查由第 8–12 关扩大到全部 12 关；功能守恒测试新增提示跨关关闭断言。
- 正式 JSON、离线关卡包和独立试玩提示已同步；Android APK 与微信小游戏工程本次暂未同步。

## 第 10 关拖字与桥面修复（网页 v169 / 试玩 v9，2026-08-28）

- 复现“路放不过去”：提示开启时，全屏场景手势屏障先于“路”字命中返回，导致提示教玩家拖字却无法抓起；现在按住仍可见的“路”字会自动关闭提示，并在同一次按下进入拖拽。
- 原放置区只接受 `y=540–820`，而断口地面线在 `y=870`，自然把字放到缺口附近会失败；改为从两岸几何计算目标区，覆盖断口与地面线。
- 原桥面顶为 `y=700`，比两岸高 170 像素，会像侧墙一样卡住人物；现在桥面由两岸几何生成，左右精确连接 `x=720–1180`，顶面与两岸同为 `y=870`，可直接走过。
- 新增 `state/help-auto-close-for-gesture`、`mechanic/word-drop` 详细坐标/拒绝原因和 `mechanic/word-bridge-created` 桥面几何日志；正式主线与独立试玩均同步。
- 独立试玩新增 `?level=10` 单关启动入口，并在启动日志记录请求关卡与实际进入关卡，便于无需改存档即可复现。
- 回归测试覆盖提示拖字例外、自然落点接受、断口外拒绝、桥两端和桥面齐平，同时继续检查第 12 关顶部 UI 不被地图拖拽吞掉。Android APK 与微信小游戏工程本次暂未同步。

## 主线选关横滑分页（网页 v170，2026-08-28）

- 主线选关从一次显示三排改为每页最多 `4 列 × 2 排`：第 1 页显示 1–8 关，第 2 页显示 9–12 关，后续关卡数量增加时继续按每 8 关自动分页。
- 手机支持左右横滑，电脑支持鼠标横向拖动与键盘左右方向键；标题、通关进度、页码和“返回营地”保持固定，拖动时仅关卡卡片跟手并显示相邻页。
- 点击关卡由按下立即进入改为松手确认；小幅手抖仍按原卡片处理，达到横滑阈值才翻页，纵向拖动、系统取消、鼠标移出和额外触点都不会误进关。
- 进入第 9–12 关后再从暂停或通关路径返回选关页，会自动保留在第 2 页；从首页重新打开主线或用 `screen=levelSelect` 进入时仍从第 1 页开始。
- 新增可独立开关的 `level-select` 诊断模块，记录手势起止、翻页、边界回弹和多指忽略；逐帧移动不写日志，避免拖慢渲染。
- 新增 `level-select-pagination-test.js`，并通过 JavaScript 语法、主线功能守恒、第 8–12 关冒烟、独立试玩冒烟及浏览器真实左右横滑/点击/返回检查。
- 本次仅更新网页主线；Android APK 与微信小游戏工程尚未同步。

## 首页正式标题（网页 v171，2026-08-28）

- 首页原两行标题“大聪明 / 脑洞蛋”改为单行居中的正式名称“怪奇之原”。
- 浏览器标签标题同步为“怪奇之原 · F4 旷野版”，资源缓存参数与代码版本升级至 v171。
- 诊断导出新增 `branding.title`，功能守恒测试新增标题常量与首页绘制位置断言；静态标题不产生逐帧日志，避免无意义噪声。
- 本次仅更新网页主线；Android APK 与微信小游戏工程尚未同步。

## 第 13–16 关第二轮独立试玩（试玩 v1，2026-08-28）

- 新建完全隔离的 `brainstorm_levels_13_16_demo`，没有修改网页主线、Android APK 或微信小游戏运行文件；启动先进入 4 张卡片组成的试玩选关页，也支持 `?level=13` 至 `?level=16` 单关复现。
- 第 13 关《进退之间》：同时按住左右约一秒，两端能量在断口中间相遇并生成实体桥；支持键盘双键和触屏双指，未满一秒松开会清空本轮进度。
- 第 14 关《薄处有光》：触屏必须用两指同时撑开亮缝两侧；电脑可以依次把左右两边向外拖，只有两侧都达到阈值才打开通路。
- 第 15 关《一笔成路》：从左断口起笔，在墨量上限内连续画到右断口；错误起点、错误落点、超量、越界和过度回头均给出明确反馈，合法墨迹落定为可通行道路。
- 第 16 关《请稍候》：看似卡在 43% 的加载画面其实是关卡，玩家控制加载条内人物跳过缺口并走到最右端，使进度升到 100%。
- 四关统一保留具体提示、重来、暂停、返回试玩选关、通关下一关、A/D/方向键、空格和多点触控；标题只保留氛围，不在画面直接泄露解法。
- 新增 `runtime`、`state`、`input`、`gesture`、`mechanic:level13–16`、`physics`、`render`、`export` 分模块结构化日志；物理日志默认关闭，内存最多 1000 条，敏感字段自动脱敏并支持 JSON 导出。
- 新增纯逻辑、日志脱敏和静态隔离冒烟测试；通过新试玩语法/冒烟、主线功能守恒、主线分页、第 8–12 关冒烟与补丁空白检查，并在浏览器实测选关、第 14 关双边拖开、第 15 关画路、第 16 关提示和暂停返回。

## 断崖视觉与第二轮试玩收敛（网页 v172 / 独立试玩 v2，2026-08-28）

- 主线分段地面统一使用 `high-cliff-v2` 高崖视觉，第 4 关、第 10 关及未来新增断口自动获得更清晰的崖壁纵深和掉落阴影；仅升级绘制，平台数据与碰撞几何保持不变。
- 新增 `render/drop-terrain-layout` 结构化诊断事件，按关卡记录地面段、断口数量、样式版本和 `collisionGeometryChanged: false`，不产生逐帧噪声。
- 独立第二轮试玩删除第 14、16 关，只保留第 13、15 关；选关、单关入口、通关顺序、日志模块、说明和回归测试同步收敛。
- 第 15 关提示改得更含蓄，移除两段虚线与画笔式明示；合法墨迹落定为顶面与两岸齐平的实体墨桥，人物明确踩在墨桥上方，不再与墨迹重叠。
- 网页主线缓存与代码版本同步升级为 v172；Android APK 与微信小游戏工程本次暂未同步。

## 第 13–42 关连续评审（独立试玩 v3，2026-08-28）

- 将独立试玩扩展为第 13–42 关共 30 个脑洞机制，难度随机；当前只用于试玩筛选，不提前写入正式主线。
- 每关右上角提供“加入主关 / 抛弃”，选择后自动进入下一关；不提供上一关、下一关、跳过或暂不决定，30 关结束后显示批量汇总。
- 评审结果仅保存在浏览器本机；每个关卡同时记录创造模式能力标签，最终被加入的机制将在统一处理阶段同步到创造模式。
- 新增双键蓄力、逃跑元素、画路、静止计时、反向控制、光影、文字实体、敲门、花朵密码、影子钥匙、载人平台、缩放、长按、真假终点、冻结障碍、镜像、重力、声音顺序和擦除墙体等机制。
- 试玩采用 `high-cliff-v3` 统一断崖 UI；补充 `review`、`storage` 与十类机制日志模块，敏感字段脱敏、1500 条内存上限和生产默认关闭高频物理日志保持有效。
- 新增 30 关目录、评审状态、纯逻辑、触控/键盘、断崖渲染及日志开关回归测试；正式网页主线仍保持 v172。

## 第二关箱子旁瞬移修复（网页 v174，2026-08-29）

- 根据“在箱子旁边卡到屏幕最左边”的复现线索核对第二关真实几何：整块地面左边界为 `x=0`，旧横向碰撞只要检测到任意 AABB 重叠，就会在向右移动时把人物中心强制设为 `0 - HALF_W = -40`。
- 该问题不是箱子出生点错误，而是人物脚底因接触/浮点误差轻微进入地面后，宽地面被误判成竖墙；旧算法随后使用整块地面的最左边界做位置纠正。
- 新增 `web_preview/physics-collision.js` 纯逻辑模块：只有人物上一帧在平台侧边外、本帧真正越过该侧边时才阻挡；已有横向重叠与平台顶面接触交给 Y 方向处理，不再产生跨屏纠正。
- `collide/horizontal-snap-prevented` 记录关卡、平台类型、原坐标、保留坐标、被拒绝的旧坐标和原因；`collide/horizontal-platform-block` 记录真实侧墙碰撞。日志仍可独立关闭，同一平台的防瞬移事件最多每秒一条。
- 新增 `web_preview/tests/level-2-crate-collision-test.js`，使用第二关地面与台阶原始尺寸固定复现 `x≈750 → x=-40`，同时验证真实台阶侧墙继续阻挡、顶面接触不误判。
- JavaScript 语法检查、第二关专项测试、选关分页、12 关冒烟和主线功能守恒测试全部通过；真实网页已加载并显示代码 v174。Android APK 与微信小游戏工程本次暂未同步。

## 自动下一关、第 7 关视差与第 8 关太阳修正（网页 v175，2026-08-29）

- 复现首次通过第 9 关却只有 9 关可见：通关流程在更新 `_progress.unlocked` 之前就计算 `nUnlocked`，导致刚刚应解锁的下一关被误判为未解锁并返回选关页。
- 调整为先写入完成记录和下一关解锁，再计算 `hasNext`；有下一关时直接载入，没有下一关时进入完成页，不再从通关路径返回选关页。新增 `state/mainline-auto-next-decision`，记录解锁前后值、文件可用性和最终决策。
- 第 7 关远山按 0.06、0.105、0.15 三档随相机产生视差；草、裂纹和浅色斑块全部以世界坐标锚定，保证向右跑时参照物向左移动、向左跑时反向。加载无限地图时记录一次 `render/infinite-parallax-config`，不写逐帧噪声。
- 第 8 关仅在关卡实际游玩画面隐藏通用背景右上太阳，保留 `_shadowSunX` 绘制的左上可拖动机关太阳；首页、选关等页面的背景不受影响。
- 正式主线数据仍为 12 关；第 13–42 关属于独立连续评审池，按既定流程只有用户选中“加入主关”并统一处理后才会进入主线。本次没有删除这些候选关卡。
- 新增 `mainline-auto-next-parallax-test.js` 固定检查解锁顺序、第 7 关双向视差、世界坐标纹理、第 8 关单太阳和评审池说明。Android APK 与微信小游戏工程暂未同步。

## 选关横滑输入兼容修复（网页 v176，2026-08-29）

- 旧实现只监听 `mousedown/mousemove/mouseup` 与传统 Touch Events，画布也未声明 `touch-action`；部分内置浏览器会先把横向拖动接管成浏览器手势，游戏收到按下却收不到完整移动/松手，因此看起来“又滑不了”。
- 新增 Pointer Events 选关专用链路，按下时调用 `setPointerCapture`，滑出卡片甚至画布边缘仍能持续收到移动和松手；`pointerup` 纳入有效翻页与轻触释放判定，`pointercancel` 只清理不翻页。
- 原 mouse/touch 监听作为兼容后备保留；统一指针手势活跃时，旧事件会作为重复指针被忽略，不会翻两页或重复打开关卡。
- 画布增加 `touch-action: none` 与文字选择禁用，阻止浏览器抢占横滑。`level-select` 日志仍独立记录 `pointer`、`dx/dy`、`reason` 和 `outcome`，不增加逐帧日志。
- 分页回归测试增加 Pointer Events、指针捕获、有效 pointerup 和 CSS 手势策略检查。Android APK 与微信小游戏工程暂未同步。

## 第 10 关标题与第 11 关箱子实体碰撞（网页 v177，2026-08-29）

- 第 10 关原先分别绘制“纸上有”和“路”，两个中心位置写死为 `W/2-42` 与 `W/2+112`，导致最后一字间距明显大于前三字，直接暗示“路”可操作。
- 新增统一四字布局：四个字按固定 64 逻辑像素步长围绕画面中心排列；“路”的拖动热区从同一布局计算，视觉位置与交互位置不会再次错开。
- 第 11 关原逻辑只把 `static_platform` 箱子加入落顶候选，却在人物横向推箱循环中直接跳过，所以可以从侧面穿箱；现复用平台横向碰撞解析器，真正越过箱子侧边时阻挡，箱顶接触仍交给纵向流程。
- 静态箱新增底面阻挡，向上跳不能穿箱；落在箱顶仍稳定选择最高候选面。重复站立不再逐帧写 `persistent-crate-land`，侧面持续按键日志每 500ms 至多一条。
- 新增 `static-crate-horizontal-block`、`static-crate-ceiling-block` 可独立筛选碰撞证据，以及第 10/11 关专项回归测试。
- 第 13–42 关文件和评审结果未删除；它们仍是独立连续评审池，按用户此前确定的流程，完成试玩并选择“加入主关”后才统一合并进正式主线。

## 评审通过关卡合并正式主线（网页 v178，2026-08-29）

- 根据评审汇总，将评审编号 `13、15、17、24、28、30、35、36、41` 按顺序重新编号为正式主线第 13–21 关；原有第 1–12 关顺序、标题、入口、暂停返回、创造模式和日志导出均保持不变。
- 新增双键生桥、一笔画路、反向控制、点击人物缩小、按住空白冻结尖刺、背对门等待、人物与门同步跳跃、点击草切换地面、来回擦除墙壁九种正式机制；标题保持含蓄，问号提示给出可直接执行的操作。
- 为每关增加独立 JSON，并同步离线 `levels_bundle.js`；正式主线总数改为 21，选关分页改为 1–8、9–16、17–21 三页。旧存档根据连续已通关编号补齐下一关解锁，不重置玩家既有记录。
- 新机制日志拆为 `mechanic:bridge`、`mechanic:gesture`、`mechanic:transform`、`mechanic:hazard`、`mechanic:direction`、`mechanic:door`、`mechanic:terrain`、`mechanic:erase`，每类可单独启停和筛选；状态日志记录正式编号、原评审编号、操作结果及非敏感几何信息，高频逐帧日志保持关闭。
- 新增评审顺序、纯逻辑交互、21 关 JSON/离线包一致性、三页横滑、旧存档迁移和日志文档回归检查。Android APK 与微信小游戏工程本次暂未同步。

## 删除原第 14 关并强化反向阶梯（网页 v179，2026-08-29）

- 删除原第 14 关「一笔成路」（评审编号 15）的正式 JSON 和离线包入口；原第 15–21 关依次前移，新的正式主线为连续第 1–20 关。
- 原第 15 关「方向借走了」成为新的第 14 关，在保留左右反向控制的基础上增加三层递升阶梯、终点平台和右上方门；提示同步要求玩家反向操作并连续跳上阶梯。
- v178 已试玩存档通过 `mainline_revision: 179` 只迁移一次：删除旧索引 13 的完成记录，后续完成索引与超过该位置的解锁上限统一减一，不重置前 13 关进度。
- 正式 JSON、离线包、20 关三页选关、缓存、测试和调试说明同步更新；阶梯仍使用既有平台碰撞和 `collide` 日志，新关卡来源继续记录 `reviewSourceId: 17`。Android APK 与微信小游戏工程本次暂未同步。

## 第 7/9 关背景稳定与旧服务纠正（网页 v180，2026-08-29）

- 第 7 关将远山位移固定为 0，只让相机世界中的地面、草、裂纹和浅色斑块反向移动，避免背景远山抢走奔跑参照感。
- 第 9 关删除 `shift % 480` 的离散山脉偏移，改用连续世界坐标格点计算 `screenX = worldX - shift`；跨越任何相机边界时只产生逐像素平滑位移，不再整片闪跳。
- `render/infinite-parallax-config` 增加 `mountainMotion` 与实际三层速度：第 7 关记录 `fixed` 和 `[0,0,0]`，第 9 关记录 `stable-world-lattice` 和 `[0.06,0.105,0.15]`。日志仍只在进入关卡时写一次，可独立关闭 `render` 模块。
- “只有12关”的代码与数据检查结果为20关完整；故障来自 8765 旧服务根目录和浏览器中的 v177 页面，不是关卡再次丢失。正式预览重新绑定当前仓库，并用右上版本号、选关总数及 `level-load/probe-complete` 三项交叉确认。
- 新增第 7 关山脉固定、第 9 关旧取模实现缺失和跨边界连续位移回归；缓存版本同步升级到 v180。Android APK 与微信小游戏工程本次暂未同步。

## 第 14 关左右交错高塔（网页 v181，2026-08-29）

- 将「方向借走了」由三层阶梯扩展为十层普通阶梯加一层终点平台；从第一层开始，后续世界方向严格按左、右交替，玩家需要在反向控制下不断切换按键。
- 每层顶面比前一层高165逻辑像素。按当前 `JUMP_VELOCITY=-1080`、`GRAVITY=2000` 计算，最大理论跳高约291.6像素，因此一层可达、连续两层330像素不可直接跨过，避免跳过主要操作步骤。
- 关卡设置 `expand_up:true`、`map_extent.up:2` 和 `world_size.h:3240`。终点平台顶面位于 `y=-945`，门底与其严格贴合；门整体位于开局首屏上方，约完成六次攀爬后才随纵向镜头进入画面，终点构图仍保持在画面中部。
- 提示明确说明左右控制相反并要求来回向上跳，但不直接透露平台按键序列。正式 JSON 与离线包保持一致。
- 新增 `mechanic:direction/reverse-climb-route-config`：进入关卡时一次性记录平台路线、左右方向、镜头上界与门的初始可见性；不增加逐帧日志。回归测试覆盖十层数量、严格交错、单跳/隔层高度、首屏隐藏和终点镜头可见。

## 第 14 关净空与路线节奏修复（网页 v182，2026-08-29）

- 根因：v181 的相邻平台顶面高度差为165像素，扣除上层平台52像素厚度和人物120像素高度后，站立净空为 `165 - 52 - 120 = -7`像素。人物在下层起跳会立即碰到上层底面，被碰撞逻辑顶回。
- 修复：十层普通阶梯与终点平台均改为200像素顶面高度差，站立净空提高到28像素。但静态净空安全不代表上升过程安全：进一步按真实60fps的移动、重力与碰撞顺序模拟后，将出生点到第一层的水平间距设为350像素，后续普通平台设为360–400像素，最后一跳到终点为380像素，延后人物进入上层平台水平投影的时机，确保进入时已上升足够高度。两层高度差为400像素，大于约291.6像素的理论最大跳高，仍不能直接跳层。
- 节奏：从第一层开始的世界方向为「左、右、右、左、左、右、左、右、左、右」，同时包含折返和连续同向两次，不再只做机械的一左一右。终点门保持首屏隐藏，约登上第七层后进入镜头。
- 日志：`mechanic:direction/reverse-climb-route-config` 增加 `segments`（每段的 `rise`、`centerSpan`、`edgeGap`、`timeToProjection`、`riseAtProjection` 与 `projectionClearance`）、`motionConstants`、`headClearances`、`minHeadClearance` 和 `minProjectionClearance`，进关时只记录一次。可单独开启 `mechanic:direction` 排查路线、静态净空与投影进入时的动态安全；不需开启高频逐帧日志，无敏感数据。
- 验证：回归固定检查每段200像素层高、至少24像素净空、从真实出生点起算的首跳350像素、后续普通平台360–400像素和终点至少380像素水平间距，并按人物半宽40、水平速度300、跳速1080、重力2000计算首次进入上层投影时的上升高度，要求不低于本段200像素层高；同时覆盖变化方向序列、不能隔层、第七层门可见及 `minHeadClearance`、`minProjectionClearance` 诊断字段。缓存版本同步升级至 v182。Android APK 与微信小游戏工程本次暂未同步。

## 创造秘境下线与微信 20 关上线准备（Web / 微信小游戏 v183，2026-08-29）

- 首页仅保留居中的“踏上旅途”，移除“创造秘境”可见入口；`CREATOR_MODE_ENABLED=false` 同时保护调试跳转、关卡码输入、自定义关卡保存/试玩和逐帧状态边界，避免通过旧状态或隐藏路径进入。
- 下线是可逆的功能关闭，不清理 `smart_egg_creator_levels_v1`，不覆盖玩家已经保存的自定义关卡。诊断快照明确记录 `creatorMode=false` 与 `localDataPreserved=true`。
- 网页主线缓存和代码升为 v183，正式关卡仍为连续20关；首页、主线选关、暂停、自动下一关、日志导出及全部现有机制保持不变。
- 新增 `feature`、`storage`、`network` 等可独立启停的日志模块；结构化上下文会递归脱敏 password、secret、authorization、cookie、accessToken、refreshToken、privateKey 等字段，高频 `physics` 在生产默认关闭，内存环形上限保持10000条。
- 微信小游戏从 v163/7关同步到 v183/20关，新增 `physics-collision.js`、`accepted-mechanics.js`，并保证关卡离线包和辅助模块与网页源逐字一致。选关为1–8、9–16、17–20三页。
- 新增 `wechat_minigame/tools/sync-web-runtime.js`：确定性注入微信隔离环境、内嵌关卡冷启动和系统键盘适配；同步和 `--check` 均输出时间、模块、文件大小及 SHA-256，方便逐文件定位漂移。
- 微信适配层保留触屏多指和 A/D/空格控制，增加 Shift 以在电脑上测试“按住空白冻结”机制。冒烟覆盖20/20关卡、创造秘境不可达且数据保留、冷启动无 fatal、三页选关、10项输入回归和原生日志导出。
- 微信适配层诊断补齐独立会话标识与递增序号，并覆盖启动/就绪、视口、前后台、网络、触摸、键盘、音频实现、存储变更及导出链路；`input`、`audio` 高频详情在生产默认关闭，按模块开启后可单独复现。敏感字段、Bearer、Token 参数和 JWT 均自动脱敏，环形缓冲上限200条。
- `project.config.json` 使用基础库3.17.1、上传自动压缩，并排除测试、工具与发布文档。当前 AppID 仍为开发者工具测试号；本次不自动上传，正式上线前必须换成《怪奇之原》正式小游戏 AppID，并完成 iOS/Android 真机、隐私、类目和审核素材核对。

## 微信小游戏正式 AppID 接入（v183，2026-08-29）

- 将微信小游戏工程的 AppID 从开发者工具测试号替换为用户确认的正式 AppID `wx6f83b5cdbf65041b`；20 关内容、创造秘境下线开关和代码版本保持 v183 不变。
- 微信开发者工具确认当前登录账号可管理该 AppID，后台名称为“怪奇之原”；正式 AppID 下重新编译后仍为20/20关、`missing_count=0`，控制台无错误。
- README、版本记录与上线检查清单同步更新；下一步仍需完成 iOS/Android 真机、隐私、类目与审核素材验收。本次不自动上传代码、不提交审核。

## 微信小游戏 v183 真机预览包（2026-08-31）

- 使用正式 AppID `wx6f83b5cdbf65041b` 生成微信小游戏真机预览二维码，预览包总大小为263,958字节。
- 二维码图片保存于系统临时目录，只用于当前扫码测试，不纳入 Git 或公开仓库，避免失效预览码长期传播。
- 本次动作不等于上传体验版，也未提交微信审核；下一步需要在 iOS、Android 真机核对横屏、安全区、多指移动跳跃、音频、20关抽样和日志导出。

## 微信小游戏 v183 预览二维码重新生成（2026-08-31）

- 用户反馈首枚真机预览码失效后，重新确认微信开发者工具登录有效、正式 AppID 正确，并刷新 v183 工程后生成第二枚全新二维码。
- 新二维码与首枚二维码的 SHA-256 不同；图片继续只保存在系统临时目录，不纳入 Git。游戏代码、20关内容和版本号未发生变化。
- 本次仍只创建预览包，没有上传体验版，也没有提交微信审核。

## 微信小游戏真机音频与跨重开诊断（v184，2026-08-31）

- 真机音频改为稳定 InnerAudio 回退：先监听 `onCanplay` 再设置资源，实际播放后计时，并在结束、超时或失败时安全停止和销毁原生实例。
- 音频创建、加载、播放和清理都与物理循环隔离；设备无声或音频接口异常时只记录错误，不再让人物移动、跳跃或碰撞进入 fatal/卡死状态。
- 新增精简故障持久化，保存 `previousFault` 和 `lastFault`，其中的 `lastAction` 记录故障前最后动作。若卡住时无法手动导出，可直接重开，在首页右上角导出带上次异常的诊断包。
- 微信适配层 `audio-core` 低频关键日志生产默认开启；高频 `audio`、`input` 详情生产默认关闭，仍可按模块独立开启、筛选与关闭。日志继续脱敏并受环形容量限制。
- 发布验收固定加入三条：第一关移动两步后跳跃；无声或模拟音频失败时仍不卡死；重开后导出并核对上次异常和最后动作。Web 与微信小游戏代码版本同步升级为 v184。
- 已刷新正式 AppID `wx6f83b5cdbf65041b` 的微信工程并生成新的 v184 真机预览二维码；预览包 273,194 字节。本次没有上传体验版，也没有提交审核。
